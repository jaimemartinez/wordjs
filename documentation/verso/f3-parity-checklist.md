# F3 · Checklist de paridad TOTAL de funcionalidades

> Mandato directo del usuario (2026-08-15): «el editor debe tener todas las funcionalidades que ya tenia nuestro custom puck editor».
> Regla del gate: CADA item se marca solo con evidencia (test de comportamiento o verificacion en navegador anotada). Un item no replicado a proposito exige decision explicita del usuario, jamas recorte silencioso.

## A. Features del wrapper actual (PuckEditor.tsx + editores de pagina/post) — 57 items

- [x] **W01** HistoryControls (L61-87): undo/redo en el header vía usePuck selector reactivo (s.history.hasPast/hasFuture) + getPuck().history.back()/forward(); iconos MSym undo/redo, deshabilitados cuando no aplica. — **Verso**: `components/verso/editor/VersoEditor.tsx` (HistoryControls sobre handle.canUndo/canRedo, misma piel); comportamiento del stack en `lib/verso/__tests__/verso-store.test.ts`.
- [x] **W02** SaveStateChip (L96-129): pill de estado guardando(spinner)/sin-guardar(ámbar)/guardado-hace-Xm; tick cada 30s (setInterval) para refrescar el relativo sin re-render del padre; aria-live='polite' SIEMPRE montado (nunca aparece+anuncia a la vez); distingue autoguardado vs guardado manual en el texto. — **Verso**: `editor/SaveStateChip.tsx` + `editor/saveChipModel.ts` (estados/textos byte-exactos, test `saveChipModel.test.ts`).
- [ ] **W03** EditorHotkeys (L138-267, sin render): listeners keydown en capture phase en window Y en el iframe del canvas (re-enganchado cada 1000ms porque AutoFrame recarga el iframe). Ctrl/Cmd+S guarda; Ctrl/Cmd+K abre paleta (ANTES del guard de 'escribiendo', funciona con foco en cualquier input); Ctrl/Cmd+Z deshace; Ctrl/Cmd+Shift+Z o Ctrl/Cmd+Y rehace; Delete elimina el bloque seleccionado; Ctrl/Cmd+D duplica; Ctrl/Cmd+C copia el bloque a localStorage (solo si no hay selección de texto activa); Ctrl/Cmd+V pega (regenIds + inserta tras la selección o al final, recordHistory:true). Todo excepto Ctrl+S/Ctrl+K se ignora mientras se escribe en input/textarea/select/[contenteditable]/.ProseMirror o hay editor inline activo (window.puckActiveEditorId). — **Verso (PARCIAL, ola 2)**: S/K/Z/Y/Supr/D con capture en window + contentWindow SIN polling (re-attach en onFrameReady) — `editor/EditorHotkeys.tsx` + `editor/hotkeyMap.ts` (test `hotkeyMap.test.ts`); guard de escritura + inlineEditingId del store. FALTA: Ctrl+C/V (clipboard de bloques, ola posterior). Ctrl+K abre el placeholder documentado de la paleta.
- [x] **W04** ViewportControls: segmented control desktop/tablet/mobile, visible solo lg+. — **Verso**: HeaderViewportControls en `editor/VersoEditor.tsx` (track surface-container, chip blanco activo, MSym 18, `hidden lg:flex`).
- [x] **W05** ActionBarOverride (override de Puck actionBar): añade Subir/Bajar a la barra estándar (label/duplicar/eliminar); usa window.__PUCK_INTERNAL_DO_NOT_USE para clamping por longitud de zona; tras mover, re-selecciona el bloque en su nuevo índice (paridad Gutenberg). — **Verso (F2)**: `overlay/ActionBar.tsx` + `overlay/actionBarCommands.ts` (clamp por modelo, sin internals; moveNode no cambia el id → la selección persiste); test `overlay/__tests__/actionBar.test.ts`.
- [ ] **W06** GuidesController: contornos discontinuos en cada bloque + overlay de medidas padding/margin en el bloque seleccionado, repintado en scroll/resize del iframe; delega en canvasGuides.ts operando sobre el Document del iframe. — **Verso (ola posterior)**: canvasGuides.ts depende del atributo `data-puck-component`; los bloques Verso marcan `data-wjs-block-id` — el botón del header existe DESHABILITADO con title documentado hasta portar el selector.
- [x] **W07** DragHint: pill flotante 'Arrastra a una zona iluminada…' mientras appState.ui.isDragging. — **Verso**: `editor/VersoEditor.tsx` (pill inverse-surface, solo mientras `state.dragPreview !== null`, pointer-events-none, mismo texto ES).
- [ ] **W08** PreviewFrame: canvas con escala real — desktop 1280px sin marco (card completa), tablet 768px/mobile 375px en marco de dispositivo escalado a la caja disponible; inyecta scrollbar delgada en el iframe; inyecta un 'contrato de documento' CSS con !important (:root/:root>body) para que overflow-x:hidden de un tema no rompa el modelo de scroll/overlay que Puck necesita; inyecta wordjs-ui.css + hoja del tema activo en orden garantizado (framework antes que tema), poll cada 700ms durante TODA la vida del editor (AutoFrame recarga el iframe y clona <link> en posiciones arbitrarias); resuelve el slug de tema activo cada 10s (detecta cambio de tema desde otra pestaña).
- [ ] **W09** Presencia/colaboración v1: heartbeat POST /api/v1/presence/:pageId cada 10s; sendBeacon en beforeunload para salir sin esperar TTL; chip ámbar '<nombre> también está editando' si hay coeditores.
- [x] **W10** Autosave: solo status==='draft' && onSave && hasChanges && !saving; espera max(8000ms, 30000ms - tiempo desde último autosave) — floor de 8s tras volverse dirty, floor de 30s entre autosaves; pasa {autosave:true}; si onSave devuelve false NO marca savedAt (pin de test: string exacto 'Math.max(8000, 30000 - (Date.now() - lastAutosaveRef.current))'). — **Verso**: efecto en `editor/VersoEditor.tsx` sobre lib/autosavePolicy (shouldRunAutosave/computeAutosaveWaitMs, ya testeados) + `editor/saveFlow.ts` con test `saveWiring.test.ts` (flag EXACTO {autosave:true}, false/excepción no estampa).
- [x] **W11** Guardado manual: llama onSave(); si devuelve false no hace nada (sin toast/savedAt); si éxito, savedAt=now, toast 4s '¡Cambios guardados con éxito!'. — **Verso**: handleManualSave en `editor/VersoEditor.tsx` + `saveFlow.runManualSave` (test `saveWiring.test.ts`); toast 4s solo en éxito manual.
- [x] **W12** Preview en vivo: si hasChanges guarda primero (ignora error de guardado), abre /preview/<slug> en pestaña nueva (rutas draft visibles solo para el autor autenticado). — **Verso**: handlePreview en `editor/VersoEditor.tsx` (misma secuencia try/ignore + window.open noopener).
- [x] **W13** Persistencia de preferencias UI en localStorage: puck_show_sidebar, puck_show_properties (cargadas al montar antes de persistir, para no pisar el default). — **Verso**: `editor/VersoEditor.tsx` (claves EXACTAS, carga-antes-de-persistir con isUiLoaded; sheets móviles en estado aparte).
- [x] **W14** Recalculo de escala del canvas 350ms tras alternar sidebar (window.dispatchEvent(new Event('resize'))) para re-medir tras la transición CSS. — **Verso**: innecesario por construcción — useAreaSize (ResizeObserver sobre el área del canvas) re-mide DURANTE la transición de anchura del panel; documentado en `editor/VersoEditor.tsx`.
- [ ] **W15** Header completo: wordmark+breadcrumb (root traducible clicable=onCancel + título), ViewportControls, HistoryControls, SaveStateChip, badge de presencia, botón Insertar (abre paleta), Reproducir animaciones, alternar Guías, alternar panel Propiedades, selector de estado draft/publish/pending, botón Vista Previa, botón Guardar/Publicar, avatar. — **Verso (PARCIAL, ola 2)**: todos los controles presentes con la piel del blueprint SALVO badge de presencia (W09, ola posterior — solo aparece con coeditores) y Guías (presente pero deshabilitado documentado, ver W06); Insertar/⌘K abre el placeholder de paleta; Reproducir reusa replayAnimations (DOM-only, engine-agnóstico).
- [ ] **W16** Rail izquierdo 64px (desktop): Bloques/Estructura(outline)/Plantillas (colapsa el panel si se re-clica la vista activa), Recursos (media library), Notas (comentarios, si pageId), Historial (revisiones, si pageId), Ajustes (deselecciona bloque → props del root). — **Verso (PARCIAL, ola 2)**: rail 64px completo con colapso-al-reclicar; Bloques/Estructura REALES; Plantillas presente con panel placeholder (ola 3); Recursos/Notas/Historial presentes DESHABILITADOS con title documentado (olas 3+); Ajustes REAL (handle.select(null) + abre panel derecho → root fields).
- [x] **W17** Panel izquierdo 280px (o sheet completo mobile): Puck.Outline si railView==='outline', si no BlockInserter. — **Verso**: panel 280px/sheet con header 40px + `inert` al colapsar; outline NUEVO (`editor/OutlineTree.tsx` sobre `editor/outlineModel.ts`, test `outlineModel.test.ts` — selección sincronizada en ambos sentidos) y `editor/BlockPalette.tsx` como inserter.
- [x] **W18** Navegación inferior mobile (4 tabs: Bloques/Capas/Propiedades/Ajustes) + FAB flotante que abre CommandPalette (oculto mientras hay un sheet abierto). — **Verso**: `editor/VersoEditor.tsx` (misma piel; el FAB abre el placeholder de paleta mientras la CommandPalette llega en ola 3).
- [ ] **W19** Estado vacío del canvas: tarjeta flotante 'Comienza tu diseño' con CTA que abre la paleta + 3 patrones rápidos (PATTERNS.slice(0,3)). — **Verso (PARCIAL, ola 2)**: tarjeta frosted + círculo 144/space_dashboard 56 + CTA píldora (abre el panel Bloques); los 3 patrones rápidos llegan con el sistema de patrones (ola 3).
- [ ] **W20** Panel de accesibilidad flotante (340px fijo derecha) alimentado por runA11yAudit sobre el Document del iframe, diferido con requestAnimationFrame.
- [x] **W21** PropertiesPanel: header con icono+label+ID mono del bloque seleccionado ('Página' sin selección); 3 pestañas REALES Contenido/Estilo/Avanzado filtradas por CSS sobre clases marcador (.wjs-f-look, .wjs-f-anim/.wjs-f-hide) probadas contra el DOM renderizado (doble timeout 50/300ms porque Puck.Fields cambia async); overlay 'Panel bloqueado' mientras isDragging; footer 'Restablecer estilos' vuelve look/anim/hide a los defaultProps del tipo de bloque (undoable). — **Verso**: `editor/PropertiesPanel.tsx`; el filtrado es EXPLÍCITO por clave (`editor/panelTabs.ts`, test `panelTabs.test.ts`) — equivalente documentado del CSS :has() porque las clases marcador viven exclusivamente en los 3 controles compartidos que withSharedVersoFields inyecta bajo esas claves (sin DOM-probe ni timeouts: Verso renderiza los campos él mismo, síncrono); overlay drag-lock sobre `state.dragPreview`; reset vía transact (undoable, contenido intacto).
- [ ] **W22** MediaPickerModal: al elegir, inserta un bloque Image al final con sourceUrl relativo (NUNCA guid, que embebe el host de subida).
- [ ] **W23** RevisionsSidebar: lista revisiones + diff LCS palabra-a-palabra con cap 1.5M (degrada a bloque simple si excede) + restaurar (window.location.reload tras revisionsApi.restore).
- [ ] **W24** ReviewComments (drawer 400px derecha, z-5000): hilo editorial interno NUNCA público, meta _wjs_review_comments, escritura vía POST /posts/:id/meta (upsert de UNA sola clave — nunca colisiona con el autosave que solo toca _puck_data); optimista con rollback en fallo; resolver/reabrir; borrar (autor o moderate_comments); formateo relativo (Ahora mismo/hace Xmin/h/d).
- [ ] **W25** A11yAudit — 7 reglas: img sin alt, jerarquía de encabezados (salto de nivel + múltiples h1), enlaces/botones sin nombre accesible, campos de formulario sin etiqueta asociada, contraste insuficiente (compone fondo efectivo subiendo ancestros, luminancia relativa WCAG, umbral 4.5:1 normal/3:1 texto grande ≥24px o ≥18.66px bold), iframes sin título, tabindex positivo; cada issue mapea a su bloque vía closest('[data-puck-component]'); click selecciona el bloque en el canvas (selectBlockById resuelve zona compuesta desde el store interno).
- [ ] **W26** Clipboard de bloques cross-página (localStorage wjs_block_clipboard) y clipboard de estilos (wjs_style_clipboard) — sobreviven entre sesiones/pestañas del navegador.
- [ ] **W27** Patrones: 9 built-in (hero/intro/features/services/stats/pricing/testimonials/faq/cta) con preview en vivo renderizado con <Render> real del propio Puck a ~24% escala; patrones de usuario en localStorage (wjs_user_patterns, máx 30, capturan la página completa); insertar añade al final con ids regenerados y recordHistory:true.
- [x] **W28** BlockInserter: búsqueda instantánea + chips de categoría (Diseño/Contenido/Medios/Marketing/Dinámicos/Más), grid de tarjetas con icono MS, drag-and-drop vía Drawer.Item del fork; modo tap-to-insert en mobile. — **Verso**: `editor/BlockPalette.tsx` (misma piel; catálogo compartido BLOCK_META/GROUP_MS_ICON; drag vía data-wjs-palette-type del DnDDriver en vez de Drawer.Item — el driver nuevo no duplica nodos; tap-to-insert siempre y cierra el sheet móvil). La pestaña Plantillas del inserter llega con los patrones (ola 3).
- [ ] **W29** CommandPalette ⌘K: acciones del editor + todos los bloques insertables, portal a document.body, navegación teclado (↑↓/Enter/Esc/Tab con focus trap), agrupado con headers. — **Verso (PLACEHOLDER, ola 2)**: Ctrl/⌘+K abre un placeholder documentado (backdrop + caja max-w-xl con la misma geometría); la paleta completa es ola 3.
- [ ] **W30** CanvasThemeTemplate: envuelve el canvas en la plantilla del tema activo (Section/Grid/partes) que la ruta pública renderizaría; resuelve cliente-side con caché de sesión (Map + promesas module-level, sobrevive remounts de AutoFrame); degrada silenciosamente a children sin envolver si no hay plantilla/backend inalcanzable; DISPLAY-ONLY, jamás toca _puck_data.
- [ ] **W31** TemplateField (_wjs_template dropdown en root.fields): opciones = plantillas reales del tema activo (GET /themes/:slug/templates); '' = jerarquía normal; valor guardado que ya no existe se sintetiza como opción visible '<valor> — Missing' en vez de desaparecer.
- [ ] **W32** puckI18n (trStr/localizeConfig): traduce reactivamente TODO el chrome del editor (categorías, labels/placeholders, texto UI) a es/en/pt vía diccionario de >250 triples, aplicado con useMemo([language]) SIN remount; defaultProps de bloques NUNCA se traducen; trStr matchea por CUALQUIERA de los 3 idiomas fuente.
- [ ] **W33** BLOCK_META/blockCatalog.ts: fuente única de icono(FA+MS)/grupo/descripción por tipo de bloque, compartida entre BlockInserter y CommandPalette.
- [ ] **W34** InlineTiptap: BubbleMenu flotante (negrita/cursiva/subrayado/tachado, enlace con buscador de contenido interno + toggle nueva pestaña, color de texto con paleta+eyedropper(EyeDropper API)+picker hex custom, resaltado, familia de fuente instalada vía /fonts, tamaño de fuente stepper 12-80px, alineación, listas —solo bloque, no heading inline—, limpiar formato); guardado continuo debounced 300ms (NUNCA cierre en blur, porque el BubbleMenu porta fuera del editable); cierre solo en click-away real (fuera de .inline-text-view Y .wjs-bubble-menu) o Escape; expone window.puckCommitActive para flush pre-guardado; intercepta pointerdown/mousedown/dragstart en el wrapper para que dnd-kit de Puck no capture el drag al seleccionar texto.
- [ ] **W35** Símbolos: acción de paleta 'Guardar bloque como símbolo' (symbolsApi.create) convierte cualquier bloque en símbolo reutilizable sincronizado (editar el símbolo cambia todas las páginas); descarta props resueltas server-side antes de guardar (resolvedPosts/resolvedFiltered/resolvedSymbolItems).
- [ ] **W36** Contrato de evento CustomEvent 'puck-editor-change' con detail=id (NUNCA {activeId}) despachado en window Y en iframe.contentWindow cada vez que activeEditorId cambia; consumido por InlineText y OverlayBlocker.
- [ ] **W37** OverlayBlocker: inyecta CSS mientras hay edición inline activa para que el overlay de selección de Puck no capture clics sobre el editor Tiptap in-situ.
- [ ] **W38** StablePuckRoot (config.root.render, MODULE-SCOPE a propósito — identidad de componente estable para que React no remonte el iframe entero al recomputar el config): expone window.puckDispatch y window.puckGetData; envuelve en PublicLayoutShell + CanvasThemeTemplate; EditorContext.Provider puentea updateComponent/activeEditorId/setActiveEditorId hacia window.parent.*.
- [ ] **W39** updateComponent: recorre RECURSIVAMENTE content + cualquier prop que sea array de componentes (slots, ej. columnas) para mergear props por id; despacha vía window.puckDispatch({type:'setData', data: transform, recordHistory:true}) — Puck v0.20 NO es controlado, mutar el mirror local nunca llega al árbol renderizado (fallback pre-dispatch: setData local + onChange).
- [ ] **W40** migrate(data, config) de @wordjs/puck aplicado al cargar initialData y al importar JSON (migra DropZones legacy a Slots).
- [x] **W41** Root fields distintos por tipo: pageConfig=title/slug/_wjs_template; postConfig=title/slug/category(custom)/allowComments(radio)/_wjs_template/seo_title/seo_description/og_image/noindex. — **Verso**: `rootFieldsPage`/`rootFieldsPost` (lib/verso/coreBlocks.tsx, gate anti-drift en verso-coreBlocks.test.ts) consumidos por pages/[id] y posts/[id] respectivamente vía la prop `rootFields` del VersoEditor; el panel derecho los edita sin selección (vista "Página"/Ajustes) con setRootProps.
- [x] **W42** PuckEditorSkeleton: réplica estructural del shell (header/sidebar/canvas) con animate-pulse mientras isLoading. — **Verso**: reutilizado tal cual por ambas páginas para AMBOS motores (isLoading y también mientras el flag de motor resuelve, 1 frame).
- [x] **W43** EditorLoadError: pantalla BLOQUEANTE (Retry/Volver) si falla la carga del post/page — el editor nunca se monta con contenido vacío guardable. — **Verso**: compartido — el gate loadError corre ANTES del ternario de motor en pages/[id] y posts/[id].
- [x] **W44** editorGuards.unhydratedSaveBlocked({isNew, loaded}): bloquea guardado (manual Y autosave) de un registro EXISTENTE cuyo contenido no hidratado con éxito; siempre permite en registro nuevo. — **Verso**: mismo handleSubmit compartido por ambos motores (el guard corre antes de leer el handle); test de wiring en `editor/__tests__/saveWiring.test.ts`.
- [x] **W45** Gracia post-mount de 800ms (mountedAtRef, DUPLICADA idéntica en pages/[id] y posts/[id]): ignora eventos onChange de inicialización (migrate/resolveData) sin comerse la primera edición real si no dispara evento de init. — **Verso**: el onChange de la rama verso usa el MISMO isWithinPostMountGrace/mountedAtRef en ambas páginas.
- [x] **W46** Rama legacy-HTML: página/post sin _puck_data envuelve su content en bloque HTMLEmbed id determinista HTMLEmbed-legacy-<id>; legacyHtmlRef conserva el HTML original como red de seguridad hasta que el bloque hace round-trip (content.length>0); si el canvas queda vacío Y legacyHtmlRef tiene valor, el guardado preserva el HTML original en vez de vaciarlo. — **Verso**: seedLegacyPuckData en la carga y applyLegacyHtmlFallback en el guardado son COMPARTIDOS (corren antes del ternario de motor); la rama verso del onChange limpia legacyHtmlRef con content>0 y no machaca el cuerpo con lienzo vacío; el fallback con handle vivo está testeado en `saveWiring.test.ts`.
- [x] **W47** Regeneración de fallback HTML (contentRef) en cada onChange, serializando bloques a HTML plano para el campo legacy `content`: pages soporta Heading/Text/Image/Button/Card/Divider/HTMLEmbed; posts SOLO Heading/Text/Image/HTMLEmbed — lógica DUPLICADA y DIVERGENTE entre ambos editores (ya hoy, no solo riesgo futuro). — **Verso**: módulo ÚNICO `lib/verso/contentFallback.ts` con el switch COMPLETO de pages, usado por ambas páginas en la rama verso; byte-igual verificado en `contentFallback.test.ts` (whitespace de Card incluido). DECISIÓN RATIFICADA (F3 ola 2): la divergencia de posts era drift accidental → resuelta hacia el lado completo. El editor legacy conserva sus switches inline intactos (mandato byte-idéntico).
- [x] **W48** Auto-generación de slug desde title (mientras no se edite manualmente), sincroniza root.props.slug tanto en el ref (pages) como en state+ref (posts). — **Verso**: el efecto de autogen es compartido (estado `slug`); en la rama verso el payload lo recoge por la cadena de fallback de handleSubmit (root.props.slug vacío → estado), sin escribir en el handle (no contamina la historia de undo).
- [x] **W49** Guardado: flush window.puckCommitActive() (editor inline abierto) → lee window.puckGetData() (store VIVO, no el mirror local — el deep-equal guard de Puck onChange puede dejar el mirror obsoleto) → payload con meta._puck_data=liveData y meta._wjs_template SIEMPRE enviado incluso '' (el backend mergea meta por clave; omitir deja stale) → POST create o PUT update, con autosave:true en el body si es autosave (servidor salta snapshot de revisión) → window.history.replaceState tras crear (sin remount) para fijar la URL a /admin/pages|posts/<id>. — **Verso**: mismo handleSubmit; la rama verso hace flush con `versoHandleRef.current.commitInline()` y lee `getData()` del HANDLE VIVO (sin mirrors ni window.*); _wjs_template siempre presente verificado con handle real en `saveWiring.test.ts`.
- [x] **W50** Contrato onSave: retorna Promise<boolean> — false=bloqueado/fallido (sin stamp de guardado, sin toast, ni alert en autosave); true/void=éxito (compat). — **Verso**: `editor/saveFlow.ts` (didSaveSucceed) + tests `saveWiring.test.ts` (false, void, excepción en background).
- [x] **W51** Guardia de acceso SOLO en pages/[id] (asimetría, NO existe en posts/[id]): can('edit_posts') redirige a /admin/account si no autorizado. — **Verso**: intacta y ANTES del ternario de motor — aplica igual a ambos; posts sigue sin guardia (asimetría preservada a propósito).
- [ ] **W52** Editor de Chrome (/admin/chrome): implementación TOTALMENTE PARALELA — monta <Puck> directo con chromeEditorConfig.tsx, layout 3 paneles fijo (Puck.Components+Puck.Outline / Puck.Preview / Puck.Fields), SIN autosave/revisiones/comentarios/a11y/InlineTiptap/CommandPalette/BlockInserter con patrones/guías/viewport-switcher/withSharedBlockFields; resuelve precedencia site option → theme chrome file → starter template; guarda PUT /api/v1/chrome/:part; valida local (parseChromeData) + servidor (400 con errors[] estructurados); confirmación de 2 pasos para restaurar y para cambiar de parte con cambios sin guardar (window.confirm NUNCA usado — congela el in-app browser).
- [x] **W53** withSharedBlockFields: envuelve TODOS los bloques (core Y de plugins runtime) con 3 campos comunes — hide (visibilidad por dispositivo, clases wjs-hide-* !important, funciona igual en SSR público y preview editor), anim (animación de entrada + 'scroll' opcional, clamp defensivo duration 100-3000ms/delay 0-3000ms), look (Appearance: fondo/borde/sombra/spacing/tipografía/hover) — wrapper único display:contents (o caja real si hay animación/estilo) para evitar reflow cuando no se usa; defaultProps nuevas: hide:{}, anim:{type:'fade-up',duration:600,delay:0}, look:{}. — **Verso (F3 ola 1, commit 4275aac)**: `lib/verso/sharedFields.tsx` (withSharedVersoFields aplicado en registerCoreBlocks; defaults byte-iguales verificados contra puckConfig en verso-coreBlocks.test.ts; clamp también en la frontera de escritura); el wrapper visual lo pone VersoBlock vía SharedBlockShell (misma implementación que el público). Plugins runtime = F4.
- [ ] **W54** useRuntimePuckConfig: fusiona bloques Puck de plugins de marketplace activos EN TIEMPO DE EJECUCIÓN; primera renderización SSR/hydration devuelve config base sin cambios (evita mismatch); memoizado por identidad estable (config nueva ⇒ Puck reconstruye el store completo Y React remonta el canvas).
- [ ] **W55** replayAnimations(doc): re-dispara animaciones de entrada sobre el Document del iframe bajo demanda (botón header + acción de paleta).
- [ ] **W56** canvasGuides.ts: setOutlineMode (contorno 1px discontinuo, sólido en hover) + showSpacingOverlay (bandas padding/margin con etiqueta px, desciende por wrappers display:contents/sole-child hasta el elemento con spacing real) — puro sobre Document, sin dependencia de React ni de Puck salvo el atributo data-puck-component.
- [ ] **W57** Suite de tests del wrapper: SOLO puckHistoryUndoRedo.test.ts ejercita comportamiento real (createAppStore() del fork, debounce 250ms de history.record, back/forward, truncado de rama redo tras nuevo cambio); puckEditorWrapper.test.ts y puckForkDivergence.test.ts son PINES DE TEXTO FUENTE (regex sobre código stripeado de comentarios) — no ejecutan el componente, se invalidan enteros con cualquier reescritura estructural.

## B. Comportamientos del motor visibles para el autor (fork) — 38 items

- [ ] **M01** Reducer con 11 tipos de acción (+ recordHistory opcional en cada una): insert, duplicate, replace, replaceRoot, reorder, move, remove, setUi, setData, set, registerZone/unregisterZone
- [ ] **M02** storeInterceptor: auto-decide si graba historial (excluye por defecto registerZone/unregisterZone/setData/setUi/set salvo recordHistory explícito) y SIEMPRE invoca onAction(action, newStatePublic, prevStatePublic) — hook público para observar cada dispatch
- [ ] **M03** Store zustand con subscribeWithSelector: state, dispatch, config, componentState (loadingCount por nodo con debounce de 50ms vía setTimeout + pendingLoadTimeouts), plugins, overrides, viewports, zoomConfig{autoZoom,rootHeight,zoom}, status LOADING→MOUNTED→READY (ciclo de vida del boot del iframe), iframe config, selectedItem derivado de ui.itemSelector
- [ ] **M04** resolveComponentData en el store: resuelve un nodo con callbacks de loading (setComponentLoading 50ms defer) y encadena refreshPermissions tras resolver
- [ ] **M05** resolveAndCommitData: recorre TODO el árbol una vez, resuelve cada nodo, y solo hace commit (replace/replaceRoot) si el nodo SIGUE existiendo tras la resolución async (guard contra borrado concurrente) y si didChange es true
- [ ] **M06** Slice history: undo/redo con debounce de grabación a 250ms, hasPast/hasFuture, back/forward despachan {type:'set'} con tidyState (limpia ui.field.focus), hotkeys cmd/ctrl+z, shift+z, y (react-hotkeys-hook)
- [ ] **M07** Slice permissions: permisos globales por defecto {drag,edit,delete,duplicate,insert}=true, resolvePermissions por item/type/root/todos, cache por id con invalidación por diff (deep-diff getChanged), fuerza refresh con force=true
- [ ] **M08** Slice nodes: registerNode/unregisterNode expone métodos imperativos {sync,showOverlay,hideOverlay} por id de nodo (usado por LayerTree para resaltar al hover)
- [ ] **M09** Slice fields: resuelve campos dinámicamente por componente seleccionado (componentConfig.resolveFields) con abort si el item seleccionado cambió durante la resolución async
- [ ] **M10** DraggableComponent: overlay portaleado (createPortal) fuera del árbol normal, useSortable de dnd-kit con collisionDetector dinámico memoizado por eje, disabled de drag/drop gateado por permisos, ResizeObserver + scroll/resize listeners para re-sincronizar posición, detección automática de eje de arrastre por display:inline computado, click selecciona (dispatch setUi itemSelector), stopPropagation si el click no viene de data-puck-overlay-portal
- [ ] **M11** DIVERGENCIA 1 (WORDJS, 4 sitios en DraggableComponent + 1 en Preview): overlay del editor portaleado al documento PADRE vía frameElement→[data-puck-preview]→[data-puck-overlay-layer] en vez del <body> del iframe; con fallback al comportamiento upstream si la capa no existe; geometría 1:1 (getBoundingClientRect crudo, sin término de scroll ni división de escala) cuando está en la capa padre; re-sync en scroll (capture:true) + resize del documento del iframe, solo mientras el overlay está visible
- [ ] **M12** DIVERGENCIA 2 (WORDJS, 1 sitio): acción 'Edit' en el ActionBar SOLO para componentType Text/Heading, antes de Duplicate, que llama window.puckSetActiveEditorId(id) — bypassa el editor de texto inline nativo de Puck (InlineTextField) en favor del editor de texto enriquecido propio de WordJS
- [ ] **M13** ActionBar: Action/Group/Label componibles, override completo vía overrides.actionBar (pero sin acceso a id/componentType — limitación de la API pública que motivó la Divergencia 2)
- [ ] **M14** DropZone dual-mode: DropZoneEdit (canvas del editor, registra zona on-mount, warning de deprecación si se usa DropZone fuera de root, acceptsTarget por allow/disallow, isEnabled solo si es la zona MÁS PROFUNDA bajo el puntero) vs DropZoneRender (modo publicado/preview, lee de renderContext, expande zones legacy vía setupZone)
- [ ] **M15** DropZoneChild: resuelve item real o preview de arrastre (shim desde ZoneStoreContext.previewIndex), aplica pipeline de field-transforms (slot-transform + inline-text-transform + transforms de plugins + transforms de usuario) antes de renderizar
- [ ] **M16** Modelo de datos slot: campos type:'slot' contienen Content (array de ComponentData) directamente dentro de props — reemplaza el modelo legacy de zonas string 'parentId:propName'
- [ ] **M17** Modelo de datos zona legacy (Data.zones: Record<zoneCompound, Content>) TODAVÍA soportado con warning de deprecación en consola
- [ ] **M18** migrate.ts: migración 1 (root bare props → root.props), migración 2 (zonas DropZone → slot) que FALLA DURO (throw) si el slot destino no existe en el config del componente; soporta migrateDynamicZonesForComponent para zonas generadas por arrays dinámicos
- [ ] **M19** resolveAllData: resolución async pura y standalone (fuera del store) de TODO el árbol incluyendo slots anidados recursivamente — para SSR/publish
- [ ] **M20** AutoField: 9 tipos de campo (text, number ambos→DefaultField; textarea; select; radio; array recursivo con getItemSummary/min/max; object recursivo; external con fetchList/mapProp/mapRow/filterFields/showSearch; custom con render prop; slot que retorna null y se resuelve fuera por DropZone), shadow local de valor mientras el campo tiene foco (AutoFieldPrivate) para no perder el caret con eco del estado global, onFocus/onBlur sincronizan ui.field.focus, NestedFieldContext propaga readOnlyFields/localName a hijos (Array/ObjectField)
- [ ] **M21** Drawer: fuente draggable de la paleta (useDraggable de dnd-kit), remonta el id de drag tras cada dragend (workaround de un bug de stale-id en dnd-kit), se registra como droppable type:'void' con collisionPriority:0 SOLO para que NestedDroppablePlugin lo respete como target de cancelación
- [ ] **M22** Render: renderer puro config+data→React sin NINGUNA dependencia de store/dnd — es el mismo camino de publicación/SSR
- [ ] **M23** SlotRender + server.tsx: variante headless (con hooks) y variante RSC-safe (sin hooks) del render de contenido de slot
- [ ] **M24** AutoFrame: crea el iframe con srcDoc fijo (#frame-root), portalea children dentro, CopyHostStyles clona TODOS los <style>/<link rel=stylesheet> del documento padre al iframe vía MutationObserver en vivo con dedupe por hash (object-hash), sincroniza atributos de <html>/<body> del padre al iframe (para clases de tema/dark-mode), tres estados de disponibilidad (onReady/onNotReady) que alimentan store.status
- [ ] **M25** Puck/index.tsx PuckProvider: construye el AppStore de zustand UNA SOLA VEZ en un useState inicializador — el prop `data` se lee solo en el mount, cambios posteriores al prop NUNCA se releen (data NO controlado); auto-colapsa sidebars en mobile <638px; selecciona el viewport inicial más cercano al ancho de ventana; blending de initialHistory; dedupe de onChange vía fast-deep-equal; dispara resolveAndCommitData() una vez al montar
- [ ] **M26** PuckLayout: layout responsive con sidebars redimensionables (useSidebarResize), auto-ocultación <638px, hotkeys globales monitorizados incluso dentro del iframe (monitorHotkeys(frameDoc)), overrides.puck permite reemplazar TODO el layout, hook de debug dev-only window.__PUCK_INTERNAL_DO_NOT_USE.appStore
- [ ] **M27** Sub-exports estáticos Puck.Components/Fields/Outline/Preview — permiten recomponer el layout con las piezas conectadas al store sin heredar el layout por defecto
- [ ] **M28** DragDropContext: envuelve DragDropProvider de dnd-kit con el preset por defecto + NestedDroppablePlugin propio, AutoScroller opcional (disableAutoScroll), ZoneStore paralelo (zustand separado del AppStore) con zoneDepthIndex/areaDepthIndex/previewIndex (ghost de arrastre) e hoveringComponent
- [ ] **M29** onDragStart/onDragOver/onBeforeDragStart/onDragEnd: distingue drag NUEVO (desde Drawer) vs EXISTENTE (reorder/move), debounce de 100ms en cambio de área con deshabilitación temporal de colisiones fallback (100ms) para evitar parpadeo en cambios de zona, inserción RTL-aware (getDeepDir), rechazo de auto-drop sobre sí mismo o descendientes, commit de datos SOLO tras dragend Y tras que el elemento dnd-kit alcance status idle (post-animación, vía @dnd-kit/state effect())
- [ ] **M30** NestedDroppablePlugin: resuelve, en cada movimiento de puntero, la zona/área MÁS PROFUNDA bajo el cursor por depth-sort — el mecanismo que hace que zonas anidadas prioricen la más interna
- [ ] **M31** Colisión dinámica: createDynamicCollisionDetector(dragAxis) — detector consciente de eje (x/y) con cálculo de midpoint-impact geométrico, variante 'directional' más simple de un solo eje
- [ ] **M32** useSensors: UN solo PointerSensor (nativo de dnd-kit, NO Mouse/Touch/Keyboard clásicos) con activationConstraints diferenciados: mouse-sobre-handle sin restricción extra, touch 200ms delay+10px tolerancia, todo lo demás 200ms delay+10px tolerancia+5px distancia
- [ ] **M33** CSS Modules vía postcss-modules + getClassNameFactory (BEM: Root, Root-descendant, Root--modifier) + un puñado de .css globales no-modulares (DraggableComponent/styles.css, Sortable/styles.css, ResizeHandle/styles.css, overlay-portal/styles.css, styles.css raíz, styles/color.css, styles/typography.css) para tokens de diseño
- [ ] **M34** Build tsup con 3 entry points: bundle/index.ts (cliente), bundle/rsc.tsx (subset RSC-safe), bundle/no-external.ts — exports condicionales react-server vs default en package.json
- [ ] **M35** API pública headless: usePuck/createUsePuck/useGetPuck sobre un store zustand ESPEJO (generateUsePuck) del AppStore real, expone appState (público, sin indexes), config, dispatch, getPermissions, refreshPermissions, selectedItem, getItemBySelector/ById, getSelectorForId, history{back,forward,setHistories,setHistoryIndex,histories,index,hasPast,hasFuture}
- [ ] **M36** lib/data/*: walkTree, walkAppState (motor de indexación state.indexes.{nodes,zones} recalculado en cada acción), mapFields (visitor genérico por field.type), flatten-data/flatten-node (vista plana con defaultProps mergeados para diffing), get-item, insert/remove/reorder/replace (primitivas de splice inmutable), set-deep (setter por dot-path), setup-zone (zones legacy→content array), strip-slots/to-component/to-root (conversores de forma), populate-ids (asigna ids faltantes), for-related-zones/get-ids-for-parent/find-zones-for-area (bookkeeping de zoneCompound), make-state-public (quita indexes antes de exponer AppState)
- [ ] **M37** Overrides públicos (10 puntos de extensión): header, actionBar, headerActions, preview, fields, fieldLabel, drawer, drawerItem, iframe, outline, componentOverlay, puck, fieldTypes — actionBar NO recibe id/componentType (limitación real que forzó la Divergencia 2)
- [ ] **M38** Contrato de generación de id: cada ComponentData recibe props.id único vía generateId() (basado en uuid) al insertar o migrar (populate-ids), usado como clave en indexes.nodes y como valor de data-puck-component/data-puck-dnd en el DOM

## Estado

**F3 ola 2 (2026-08-15) — wrapper VersoEditor núcleo.** Marcado SOLO lo evidenciado con test de node
o código verificable (la verificación visual pantalla-a-pantalla queda para el gate de navegador del
orquestador). Nuevo en esta ola:

- `frontend/src/components/verso/editor/` — VersoEditor.tsx (chrome completo del blueprint),
  SaveStateChip, EditorHotkeys, BlockPalette, OutlineTree, PropertiesPanel + módulos puros
  panelTabs/saveChipModel/hotkeyMap/outlineModel/saveFlow (todos con test).
- `frontend/src/lib/editorEngine.ts` — flag de motor (query ?engine= > localStorage
  wjs_editor_engine > NEXT_PUBLIC_WORDJS_EDITOR_ENGINE > legacy DEFAULT ABSOLUTO), matriz testeada.
- `frontend/src/lib/verso/contentFallback.ts` — fallback HTML compartido (W47 resuelto al lado
  completo, byte-igual testeado).
- pages/[id] y posts/[id]: ternario de motor — rama legacy INTACTA (mismo árbol, mismas props);
  rama verso con las mismas props de datos + guards compartidos + versoHandleRef (getData vivo).
- Mecanismos legacy que Verso SUPERSEDE por arquitectura (no se portan 1:1, el contrato queda
  cubierto por el store): W36 (CustomEvent puck-editor-change → `state.inlineEditingId` +
  suscripción), W38/W39 (window.puckDispatch/puckGetData/updateComponent → EditorHandle vía
  `handleRef` + transact/setProps recursivo por diseño del doc normalizado), W40 (migrate → 
  normalize.ts con round-trip fail-soft, gate verso-roundtrip.test.ts).

Pendiente para olas 3+ (placeholders presentes y documentados, nunca omitidos en silencio):
patrones/plantillas + quick-picks del estado vacío (W19/W27), media library (W22), CommandPalette
completa (W29), clipboard Ctrl+C/V (resto de W03), guías (W06/W56), presencia (W09), a11y
(W20/W25), revisiones (W23), notas (W24), símbolos-acción de paleta (W35), CanvasThemeTemplate en
el canvas Verso (W30/W31 — TemplateField ya vive en los root fields), i18n localizeConfig sobre el
registry completo (W32 — el chrome Verso ya pasa todo por trStr), InlineTiptap completo (W34 — la
base VersoInline es de F2), chrome editor (W52, fuera del alcance del wrapper).

La seccion B se valida en el lab/e2e (muchos items ya cubiertos por F2 — marcar con referencia al
commit/test).
