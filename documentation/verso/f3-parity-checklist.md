# F3 · Checklist de paridad TOTAL de funcionalidades

> **HISTORICAL — the completion log of the F3 parity gate, written wave by wave.** Each item's
> "legacy" half describes `PuckEditor.tsx` and the surrounding fork, all since deleted; the "Verso"
> half names files that mostly still exist under `frontend/src/components/verso/`. Two mechanisms it
> relies on are gone: the engine flag (`frontend/src/lib/editorEngine.ts`, `EngineToggle`, the
> `?engine=` query and the `<Puck>` legacy branch inside the ternaries) was removed with the fork, so
> Verso is now unconditional; and the shared libraries it calls `lib/puckPatterns` and `puckI18n` are
> now `lib/blockPatterns.ts` and `lib/editorI18n.ts`. Kept as the record of the gate, not as a map of
> the current tree.

> Mandato directo del usuario (2026-08-15): «el editor debe tener todas las funcionalidades que ya tenia nuestro custom puck editor».
> Regla del gate: CADA item se marca solo con evidencia (test de comportamiento o verificacion en navegador anotada). Un item no replicado a proposito exige decision explicita del usuario, jamas recorte silencioso.

## A. Features del wrapper actual (PuckEditor.tsx + editores de pagina/post) — 57 items

- [x] **W01** HistoryControls (L61-87): undo/redo en el header vía usePuck selector reactivo (s.history.hasPast/hasFuture) + getPuck().history.back()/forward(); iconos MSym undo/redo, deshabilitados cuando no aplica. — **Verso**: `components/verso/editor/VersoEditor.tsx` (HistoryControls sobre handle.canUndo/canRedo, misma piel); comportamiento del stack en `lib/verso/__tests__/verso-store.test.ts`.
- [x] **W02** SaveStateChip (L96-129): pill de estado guardando(spinner)/sin-guardar(ámbar)/guardado-hace-Xm; tick cada 30s (setInterval) para refrescar el relativo sin re-render del padre; aria-live='polite' SIEMPRE montado (nunca aparece+anuncia a la vez); distingue autoguardado vs guardado manual en el texto. — **Verso**: `editor/SaveStateChip.tsx` + `editor/saveChipModel.ts` (estados/textos byte-exactos, test `saveChipModel.test.ts`).
- [x] **W03** EditorHotkeys (L138-267, sin render): listeners keydown en capture phase en window Y en el iframe del canvas (re-enganchado cada 1000ms porque AutoFrame recarga el iframe). Ctrl/Cmd+S guarda; Ctrl/Cmd+K abre paleta (ANTES del guard de 'escribiendo', funciona con foco en cualquier input); Ctrl/Cmd+Z deshace; Ctrl/Cmd+Shift+Z o Ctrl/Cmd+Y rehace; Delete elimina el bloque seleccionado; Ctrl/Cmd+D duplica; Ctrl/Cmd+C copia el bloque a localStorage (solo si no hay selección de texto activa); Ctrl/Cmd+V pega (regenIds + inserta tras la selección o al final, recordHistory:true). Todo excepto Ctrl+S/Ctrl+K se ignora mientras se escribe en input/textarea/select/[contenteditable]/.ProseMirror o hay editor inline activo (window.puckActiveEditorId). — **Verso (COMPLETO, ola 3)**: S/K/Z/Y/Supr/D con capture en window + contentWindow SIN polling (re-attach en onFrameReady) — `editor/EditorHotkeys.tsx` + `editor/hotkeyMap.ts` (test `hotkeyMap.test.ts`, incluye C/V); guard de escritura + inlineEditingId del store. Ctrl+C copia el SUBTREE seleccionado (subtreeToItem) a `wjs_block_clipboard` solo sin selección de texto (leída en la ventana del evento — también el iframe) y SIN preventDefault; Ctrl+V pega con ids regenerados recursivamente (regenIds del legacy) tras la selección o al final, UNA transacción = un undo — `editor/blockClipboard.ts`, tests `blockClipboard.test.ts` (posición, historia, cross-editor). Ctrl+K abre la CommandPalette REAL (W29).
- [x] **W04** ViewportControls: segmented control desktop/tablet/mobile, visible solo lg+. — **Verso**: HeaderViewportControls en `editor/VersoEditor.tsx` (track surface-container, chip blanco activo, MSym 18, `hidden lg:flex`).
- [x] **W05** ActionBarOverride (override de Puck actionBar): añade Subir/Bajar a la barra estándar (label/duplicar/eliminar); usa window.__PUCK_INTERNAL_DO_NOT_USE para clamping por longitud de zona; tras mover, re-selecciona el bloque en su nuevo índice (paridad Gutenberg). — **Verso (F2)**: `overlay/ActionBar.tsx` + `overlay/actionBarCommands.ts` (clamp por modelo, sin internals; moveNode no cambia el id → la selección persiste); test `overlay/__tests__/actionBar.test.ts`.
- [x] **W06** GuidesController: contornos discontinuos en cada bloque + overlay de medidas padding/margin en el bloque seleccionado, repintado en scroll/resize del iframe; delega en canvasGuides.ts operando sobre el Document del iframe. — **Verso (ola 4)**: `VersoGuidesController` en `editor/VersoEditor.tsx` sobre el canvasGuides COMPARTIDO (ver W56 — parametrizado por atributo, default legacy intacto): outline `[data-wjs-block-id]`, overlay de medidas del seleccionado (leído del store, no de usePuck), repintado en scroll capture + resize del iframe, limpieza al apagar; botón del header ACTIVO (aria-pressed, misma piel) + fila de paleta `guides` (test `paletteActions.test.ts`). El pintado real es DOM-only → verificación visual en el gate de navegador.
- [x] **W07** DragHint: pill flotante 'Arrastra a una zona iluminada…' mientras appState.ui.isDragging. — **Verso**: `editor/VersoEditor.tsx` (pill inverse-surface, solo mientras `state.dragPreview !== null`, pointer-events-none, mismo texto ES).
- [x] **W08** PreviewFrame: canvas con escala real — desktop 1280px sin marco (card completa), tablet 768px/mobile 375px en marco de dispositivo escalado a la caja disponible; inyecta scrollbar delgada en el iframe; inyecta un 'contrato de documento' CSS con !important (:root/:root>body) para que overflow-x:hidden de un tema no rompa el modelo de scroll/overlay que Puck necesita; inyecta wordjs-ui.css + hoja del tema activo en orden garantizado (framework antes que tema), poll cada 700ms durante TODA la vida del editor (AutoFrame recarga el iframe y clona <link> en posiciones arbitrarias); resuelve el slug de tema activo cada 10s (detecta cambio de tema desde otra pestaña). — **Verso (COMPLETO, ola 5)**: escala/marcos/bisel en `editor/VersoEditor.tsx` (mismos números DEVICE_WIDTHS, `viewport.test.ts`); el CSS lo POSEE el documento del iframe (`app/admin/canvas-frame/page.tsx`): contrato :root/:root>body, wordjs-ui.css ANTES que el tema por orden de server-render (sin CopyHostStyles → el poll de 700ms es innecesario POR CONSTRUCCIÓN — no hay AutoFrame que clone links) y, nuevo en ola 5, scrollbar delgada BYTE-IGUAL al legacy (PuckEditor L409-413) + re-resolución del tema activo cada 10s con swap imperativo sin FOUC (link nuevo `current.after()` → onload → retirar el viejo; el orden framework→tema se conserva). Cotejo visual pantalla-a-pantalla → gate de navegador.
- [x] **W09** Presencia/colaboración v1: heartbeat POST /api/v1/presence/:pageId cada 10s; sendBeacon en beforeunload para salir sin esperar TTL; chip ámbar '<nombre> también está editando' si hay coeditores. — **Verso (ola 4)**: `editor/presence.ts` COMO MÓDULO (la crítica del blueprint al inline legacy): ping inmediato + interval 10s, beacon de leave en beforeunload Y en stop, tick !ok/offline conserva el último estado, guard `dead` contra el ping en vuelo tras parar — test `presence.test.ts` (timers falsos + deps inyectadas, 5 casos). Chip ámbar idéntico al legacy en el header (`VersoEditor.tsx`), singular/plural con las tripletas existentes.
- [x] **W10** Autosave: solo status==='draft' && onSave && hasChanges && !saving; espera max(8000ms, 30000ms - tiempo desde último autosave) — floor de 8s tras volverse dirty, floor de 30s entre autosaves; pasa {autosave:true}; si onSave devuelve false NO marca savedAt (pin de test: string exacto 'Math.max(8000, 30000 - (Date.now() - lastAutosaveRef.current))'). — **Verso**: efecto en `editor/VersoEditor.tsx` sobre lib/autosavePolicy (shouldRunAutosave/computeAutosaveWaitMs, ya testeados) + `editor/saveFlow.ts` con test `saveWiring.test.ts` (flag EXACTO {autosave:true}, false/excepción no estampa).
- [x] **W11** Guardado manual: llama onSave(); si devuelve false no hace nada (sin toast/savedAt); si éxito, savedAt=now, toast 4s '¡Cambios guardados con éxito!'. — **Verso**: handleManualSave en `editor/VersoEditor.tsx` + `saveFlow.runManualSave` (test `saveWiring.test.ts`); toast 4s solo en éxito manual.
- [x] **W12** Preview en vivo: si hasChanges guarda primero (ignora error de guardado), abre /preview/<slug> en pestaña nueva (rutas draft visibles solo para el autor autenticado). — **Verso**: handlePreview en `editor/VersoEditor.tsx` (misma secuencia try/ignore + window.open noopener).
- [x] **W13** Persistencia de preferencias UI en localStorage: puck_show_sidebar, puck_show_properties (cargadas al montar antes de persistir, para no pisar el default). — **Verso**: `editor/VersoEditor.tsx` (claves EXACTAS, carga-antes-de-persistir con isUiLoaded; sheets móviles en estado aparte).
- [x] **W14** Recalculo de escala del canvas 350ms tras alternar sidebar (window.dispatchEvent(new Event('resize'))) para re-medir tras la transición CSS. — **Verso**: innecesario por construcción — useAreaSize (ResizeObserver sobre el área del canvas) re-mide DURANTE la transición de anchura del panel; documentado en `editor/VersoEditor.tsx`.
- [x] **W15** Header completo: wordmark+breadcrumb (root traducible clicable=onCancel + título), ViewportControls, HistoryControls, SaveStateChip, badge de presencia, botón Insertar (abre paleta), Reproducir animaciones, alternar Guías, alternar panel Propiedades, selector de estado draft/publish/pending, botón Vista Previa, botón Guardar/Publicar, avatar. — **Verso (COMPLETO, ola 4)**: los dos huecos de la ola 2 cerrados — badge de presencia REAL (W09, chip ámbar solo con coeditores) y Guías ACTIVO (W06); el resto sin cambios desde la ola 2/3.
- [x] **W16** Rail izquierdo 64px (desktop): Bloques/Estructura(outline)/Plantillas (colapsa el panel si se re-clica la vista activa), Recursos (media library), Notas (comentarios, si pageId), Historial (revisiones, si pageId), Ajustes (deselecciona bloque → props del root). — **Verso (COMPLETO, ola 4)**: Recursos abre MediaPickerModal (W22), Notas alterna ReviewComments (W24, solo pageId), Historial alterna RevisionsSidebar (W23, solo pageId) — mismos glifos/labels/aria-pressed/estados activos que el rail legacy (PuckEditor L1910-1962); el resto ya real desde la ola 3.
- [x] **W17** Panel izquierdo 280px (o sheet completo mobile): Puck.Outline si railView==='outline', si no BlockInserter. — **Verso**: panel 280px/sheet con header 40px + `inert` al colapsar; outline NUEVO (`editor/OutlineTree.tsx` sobre `editor/outlineModel.ts`, test `outlineModel.test.ts` — selección sincronizada en ambos sentidos) y `editor/BlockPalette.tsx` como inserter.
- [x] **W18** Navegación inferior mobile (4 tabs: Bloques/Capas/Propiedades/Ajustes) + FAB flotante que abre CommandPalette (oculto mientras hay un sheet abierto). — **Verso**: `editor/VersoEditor.tsx` (misma piel; el FAB abre el placeholder de paleta mientras la CommandPalette llega en ola 3).
- [x] **W19** Estado vacío del canvas: tarjeta flotante 'Comienza tu diseño' con CTA que abre la paleta + 3 patrones rápidos (PATTERNS.slice(0,3)). — **Verso (COMPLETO, ola 3)**: tarjeta frosted + círculo 144/space_dashboard 56 + CTA píldora que ahora abre la CommandPalette REAL (paridad exacta con el legacy, que abre ⌘K) + fila de 3 quick-picks `PATTERNS.slice(0,3)` (mismo catálogo compartido de lib/puckPatterns) sobre `insertVersoPattern` — un patrón = UNA entrada de undo (test `patterns.test.ts`); clases pointer-events idénticas al legacy (`editor/VersoEditor.tsx`).
- [x] **W20** Panel de accesibilidad flotante (340px fijo derecha) alimentado por runA11yAudit sobre el Document del iframe, diferido con requestAnimationFrame. — **Verso (ola 4)**: drawer con la MISMA piel/z-90 en `editor/VersoEditor.tsx` reutilizando el `A11yPanel` compartido; runAudit difiere con requestAnimationFrame sobre el frameDoc vivo (sin querySelector del iframe: el doc llega por onFrameReady); se abre por la fila de paleta `a11y` (paridad legacy: no hay botón dedicado).
- [x] **W21** PropertiesPanel: header con icono+label+ID mono del bloque seleccionado ('Página' sin selección); 3 pestañas REALES Contenido/Estilo/Avanzado filtradas por CSS sobre clases marcador (.wjs-f-look, .wjs-f-anim/.wjs-f-hide) probadas contra el DOM renderizado (doble timeout 50/300ms porque Puck.Fields cambia async); overlay 'Panel bloqueado' mientras isDragging; footer 'Restablecer estilos' vuelve look/anim/hide a los defaultProps del tipo de bloque (undoable). — **Verso**: `editor/PropertiesPanel.tsx`; el filtrado es EXPLÍCITO por clave (`editor/panelTabs.ts`, test `panelTabs.test.ts`) — equivalente documentado del CSS :has() porque las clases marcador viven exclusivamente en los 3 controles compartidos que withSharedVersoFields inyecta bajo esas claves (sin DOM-probe ni timeouts: Verso renderiza los campos él mismo, síncrono); overlay drag-lock sobre `state.dragPreview`; reset vía transact (undoable, contenido intacto).
- [x] **W22** MediaPickerModal: al elegir, inserta un bloque Image al final con sourceUrl relativo (NUNCA guid, que embebe el host de subida). — **Verso (ola 4)**: MediaPickerModal REUTILIZADO tal cual (auditado: agnóstico — solo MediaItem + I18n + MediaLibrarySelector, cero deps de Puck); `insertMediaItem` en `VersoEditor.tsx` inserta Image al FINAL con `src: item.sourceUrl || item.guid` y `alt: item.title` (byte-paridad con el onSelect legacy L2168-2177), defaults reales del registry, UNA transacción = un undo. ADEMÁS: `renderExternalPicker` de los campos `external` cableado al MISMO modal con `rememberPickedMedia(item)` antes de onSelect (la derivación de srcSet del legacy, puckConfig L930) — `PropertiesPanel` lo propaga a todos los VersoFieldControl (hoy ningún bloque core usa `external`; es el contrato listo para F4).
- [x] **W23** RevisionsSidebar: lista revisiones + diff LCS palabra-a-palabra con cap 1.5M (degrada a bloque simple si excede) + restaurar (window.location.reload tras revisionsApi.restore). — **Verso (ola 4)**: RevisionsSidebar REUTILIZADO tal cual (agnóstico: revisionsApi/usersApi/date-fns/useModal, cero Puck) desde la pestaña Historial del rail + fila de paleta `revisions` (solo pageId); `handleRestore` en `VersoEditor.tsx` réplica del legacy (restore → reload; error → alert con el MISMO string). El diff/confirmación viven dentro del sidebar compartido — sin cambios.
- [x] **W24** ReviewComments (drawer 400px derecha, z-5000): hilo editorial interno NUNCA público, meta _wjs_review_comments, escritura vía POST /posts/:id/meta (upsert de UNA sola clave — nunca colisiona con el autosave que solo toca _puck_data); optimista con rollback en fallo; resolver/reabrir; borrar (autor o moderate_comments); formateo relativo (Ahora mismo/hace Xmin/h/d). — **Verso (ola 4)**: ReviewComments REUTILIZADO tal cual (auditado agnóstico: postsApi/apiPost + contexts, cero Puck — el contrato anti-race POST /posts/:id/meta vive DENTRO del componente compartido, jamás el PUT general) desde la pestaña Notas del rail + fila de paleta `comments` (solo pageId).
- [x] **W25** A11yAudit — 7 reglas: img sin alt, jerarquía de encabezados (salto de nivel + múltiples h1), enlaces/botones sin nombre accesible, campos de formulario sin etiqueta asociada, contraste insuficiente (compone fondo efectivo subiendo ancestros, luminancia relativa WCAG, umbral 4.5:1 normal/3:1 texto grande ≥24px o ≥18.66px bold), iframes sin título, tabindex positivo; cada issue mapea a su bloque vía closest('[data-puck-component]'); click selecciona el bloque en el canvas (selectBlockById resuelve zona compuesta desde el store interno). — **Verso (ola 4)**: las 7 reglas NO se duplican — `runA11yAudit` compartido parametrizado por atributos (default legacy INTACTO; llamadores existentes sin cambios) y `editor/a11y.ts` lo re-apunta a `data-wjs-block-id`; el click selecciona DIRECTO con `handle.select(blockId)` (el id del atributo ES la clave del nodo — el selectBlockById de zona compuesta del legacy queda superseded por diseño). Test `a11y.test.ts` (DOM stub mínimo, sin jsdom): mapeo issue→bloque (img-alt, accessible-name, heading-order), issue fuera de bloque → blockId undefined, scaffolding de guías saltado; la regla de CONTRASTE exige getComputedStyle real → gate de navegador (documentado en el test).
- [x] **W26** Clipboard de bloques cross-página (localStorage wjs_block_clipboard) y clipboard de estilos (wjs_style_clipboard) — sobreviven entre sesiones/pestañas del navegador. — **Verso (ola 3)**: `editor/blockClipboard.ts` — MISMAS claves y MISMA forma que el legacy (bloques: item Puck crudo `{type,props}` con hijos en props; estilos: `{look,anim,hide}`), interoperable en ambos sentidos con el editor viejo (misma clave = gratis; la forma está testeada contra el PRODUCTOR real del legacy — fixture generado con buildPatternBlocks de lib/puckPatterns — en `blockClipboard.test.ts`: regeneración recursiva de ids sin repetidos, validación del lector legacy, pegar estilos vía setProps en un undo con contenido intacto). Ctrl+C/V en EditorHotkeys (W03); copiar/pegar estilos también como filas de la paleta (W29).
- [x] **W27** Patrones: 9 built-in (hero/intro/features/services/stats/pricing/testimonials/faq/cta) con preview en vivo renderizado con <Render> real del propio Puck a ~24% escala; patrones de usuario en localStorage (wjs_user_patterns, máx 30, capturan la página completa); insertar añade al final con ids regenerados y recordHistory:true. — **Verso (COMPLETO, ola 5)**: todo lo de la ola 3 (patterns.ts/PatternsPanel — fuente única con lib/puckPatterns, cap 30, un undo por inserción, tests `patterns.test.ts`) MÁS la miniatura EN VIVO: `VersoPatternPreview` en `editor/PatternsPanel.tsx` — misma anatomía que el PatternPreview legacy (caja 110px blanca, contenido a 1180px con scale(0.236), fade inferior, pointer-events-none + aria-hidden) renderizada con `RenderSubtree` (el switch público COMPARTIDO de ContentRenderer — cero dependencia del fork; la miniatura enseña el MISMO markup wp-block-* que aterrizará). Built-ins previsualizan los items construidos contra el registry; los de usuario previsualizan EXACTAMENTE lo que la inserción usará (`userPatternItems` — mismo filtro de tipos, test en `patterns.test.ts`). Cotejo visual → gate de navegador.
- [x] **W28** BlockInserter: búsqueda instantánea + chips de categoría (Diseño/Contenido/Medios/Marketing/Dinámicos/Más), grid de tarjetas con icono MS, drag-and-drop vía Drawer.Item del fork; modo tap-to-insert en mobile. — **Verso**: `editor/BlockPalette.tsx` (misma piel; catálogo compartido BLOCK_META/GROUP_MS_ICON; drag vía data-wjs-palette-type del DnDDriver en vez de Drawer.Item — el driver nuevo no duplica nodos; tap-to-insert siempre y cierra el sheet móvil). La pestaña Plantillas del inserter llega con los patrones (ola 3).
- [x] **W29** CommandPalette ⌘K: acciones del editor + todos los bloques insertables, portal a document.body, navegación teclado (↑↓/Enter/Esc/Tab con focus trap), agrupado con headers. — **Verso (COMPLETO, ola 3)**: `editor/VersoCommandPalette.tsx` (misma piel: backdrop rgba(27,27,34,0.4)+blur, max-w-xl/70vh, fila activa surface-container + trailing hint sm:, footer hints + versión real de la app; bloques del catálogo compartido getBlockItems/BLOCK_META; MEJORA sobre el legacy: ARIA de combobox correcto — input role=combobox + aria-activedescendant, listbox/option — con el foco siempre en el input) + `editor/paletteActions.ts` (builder PURO con la espec del paletteActions legacy: save/preview condicionales con labels por status, export JSON del handle vivo, import JSON validado + confirm modal + `replaceData` = UNA entrada de undo, copiar/pegar estilos wjs_style_clipboard, duplicar Ctrl+D / eliminar Supr / subir / bajar leyendo la selección PEREZOSAMENTE, ajustes de página → deselección + panel root, Ver estructura → outline del rail, replay de animaciones; media/revisiones/comentarios/a11y/guías/símbolos llegarán con sus paneles — documentado en el módulo, jamás filas muertas). Tests `paletteActions.test.ts` (comandos reales sobre createEditor, hints/glifos pinned, import de 1 undo con forma inválida rechazada); strings nuevos con tripleta ES/EN/PT en puckI18n ("Subir bloque"/"Bajar bloque"/"Ver estructura").
- [x] **W30** CanvasThemeTemplate: envuelve el canvas en la plantilla del tema activo (Section/Grid/partes) que la ruta pública renderizaría; resuelve cliente-side con caché de sesión (Map + promesas module-level, sobrevive remounts de AutoFrame); degrada silenciosamente a children sin envolver si no hay plantilla/backend inalcanzable; DISPLAY-ONLY, jamás toca _puck_data. — **Verso (ola 4)**: `canvas/VersoThemeTemplate.tsx` envuelve a EditorRenderer dentro del portal del FrameController (el iframe ya carga wordjs-ui.css + hoja del tema → markup wp-block-* byte-idéntico con sus tokens); MISMA cadena `canvasTemplateCandidates`/TEMPLATE_NAME fail-closed y MISMO TemplateRenderer/decorateForCanvas que el resolver público y el legacy; cachés de sesión module-level; el slot raíz del editor = hueco PageContent. DIFERENCIA POR ARQUITECTURA (documentada en el módulo): sin CanvasTemplateContext — kind/postType son props (`templateKind`/`templatePostType` cableados en pages/[id] y posts/[id], solo rama verso) y el `_wjs_template` se lee EN VIVO del root del store → el dropdown del panel root re-envuelve al instante por construcción (setRootProps → slice → re-resolución). Test `versoThemeTemplate.test.ts` (resolución pura espejo del resolver: hoisting del pick, fail-closed de nombre y de fichero, first-hit-wins, degradación a null).
- [x] **W31** TemplateField (_wjs_template dropdown en root.fields): opciones = plantillas reales del tema activo (GET /themes/:slug/templates); '' = jerarquía normal; valor guardado que ya no existe se sintetiza como opción visible '<valor> — Missing' en vez de desaparecer. — **Verso**: el TemplateField REAL de puckConfig se REUTILIZA como campo custom `_wjs_template` en `rootFieldsPage`/`rootFieldsPost` (lib/verso/coreBlocks.tsx — una sola implementación, dos registros; gate anti-drift en verso-coreBlocks.test.ts); el panel root lo edita vía setRootProps y, con W30, el canvas re-envuelve EN VIVO al elegir (contrato del sistema de temas). El comportamiento interno del dropdown (opciones reales + síntesis Missing) es del componente compartido, sin cambios.
- [x] **W32** puckI18n (trStr/localizeConfig): traduce reactivamente TODO el chrome del editor (categorías, labels/placeholders, texto UI) a es/en/pt vía diccionario de >250 triples, aplicado con useMemo([language]) SIN remount; defaultProps de bloques NUNCA se traducen; trStr matchea por CUALQUIERA de los 3 idiomas fuente. — **Verso (ola 5)**: el MISMO diccionario/trStr en cada string del chrome (VersoEditor/paletas/paneles, reactivo por `language` de useI18n, sin remount); `localizeConfig` queda superseded POR ARQUITECTURA (no hay objeto config que localizar): su trabajo se hace en los puntos de display — labels de bloque pasados por trStr en `BlockPalette` y `VersoCommandPalette` (nuevo en ola 5: reactivos al idioma sin remontar el registry, y la búsqueda matchea lo que se enseña), labels de campos por trStr en `PropertiesPanel`/`VersoFieldControl` (L156/L52), grupos/categorías ya trStr. defaultProps JAMÁS se traducen (el registry es datos; el gate anti-drift verso-coreBlocks.test.ts los compara byte-a-byte contra puckConfig).
- [x] **W33** BLOCK_META/blockCatalog.ts: fuente única de icono(FA+MS)/grupo/descripción por tipo de bloque, compartida entre BlockInserter y CommandPalette. — **Verso**: el MISMO módulo compartido lib/blockCatalog (BLOCK_META/GROUP_ORDER/GROUP_MS_ICON/getBlockItems) alimenta `BlockPalette` (rail) y `VersoCommandPalette` (⌘K) — las dos paletas de ambos editores presentan bloques idénticos por construcción; tipos sin meta caen al grupo "Más" con glifo widgets (mismo fallback).
- [ ] **W34** InlineTiptap: BubbleMenu flotante (negrita/cursiva/subrayado/tachado, enlace con buscador de contenido interno + toggle nueva pestaña, color de texto con paleta+eyedropper(EyeDropper API)+picker hex custom, resaltado, familia de fuente instalada vía /fonts, tamaño de fuente stepper 12-80px, alineación, listas —solo bloque, no heading inline—, limpiar formato); guardado continuo debounced 300ms (NUNCA cierre en blur, porque el BubbleMenu porta fuera del editable); cierre solo en click-away real (fuera de .inline-text-view Y .wjs-bubble-menu) o Escape; expone window.puckCommitActive para flush pre-guardado; intercepta pointerdown/mousedown/dragstart en el wrapper para que dnd-kit de Puck no capture el drag al seleccionar texto. — **Verso (PARCIAL — hueco justificado, ver gaps)**: la base VersoInline (F2) cubre entrar/salir de edición in-situ, commitInline como flush pre-guardado y dimming del resto del canvas; el BubbleMenu completo (enlace con buscador interno, color+eyedropper, fuente/tamaño, listas, limpiar formato, guardado debounced 300ms, cierre solo click-away real) NO está portado. ADEMÁS existe la tarea F3.5 (motor de texto inline PROPIO reemplazando Tiptap) — portar el BubbleMenu Tiptap ahora sería trabajo desechable: exige decisión de secuencia (portar sobre Tiptap vs esperar F3.5).
- [x] **W35** Símbolos: acción de paleta 'Guardar bloque como símbolo' (symbolsApi.create) convierte cualquier bloque en símbolo reutilizable sincronizado (editar el símbolo cambia todas las páginas); descarta props resueltas server-side antes de guardar (resolvedPosts/resolvedFiltered/resolvedSymbolItems). — **Verso (COMPLETO, ola 5)**: fila `save-symbol` REAL en la paleta ⌘K (mismo id/glifo collections/label/posición que el legacy L1571-1593) sobre `saveSelectedAsSymbol` (`editor/paletteActions.ts`): serializa el SUBTREE seleccionado con `subtreeToItem` (slots incluidos — la MISMA forma cruda que el resolver de símbolos consume), descarta las 3 props resueltas (SYMBOL_RESOLVED_PROPS), nombra `<label traducido> · <HH:MM>`, guard anti-anidamiento (seleccionar un Symbol = no-op), toast "Símbolo guardado" / alert "No se pudo guardar el símbolo" (modal, jamás window.confirm) — tests en `paletteActions.test.ts` (subtree completo, strip, skipped, error, posición de la fila, historia intacta). FLUJO COMPLETO verificado por código: insertar Symbol desde cualquiera de las dos paletas (registrado en coreBlocks L953 + BLOCK_META) → picker (`symbolBlockFields` REUTILIZADO como campo custom, VersoFieldControl case "custom") → `VersoSymbolRender` con isEditing:true (VersoBlock L82) renderiza el subtree vía RenderSubtree con `exclude={Symbol}` = cap de profundidad 1. Click real en navegador → gate del orquestador.
- [x] **W36** Contrato de evento CustomEvent 'puck-editor-change' con detail=id (NUNCA {activeId}) despachado en window Y en iframe.contentWindow cada vez que activeEditorId cambia; consumido por InlineText y OverlayBlocker. — **Verso**: SUPERSEDED POR ARQUITECTURA (ratificado en ola 2): el estado de edición inline vive en el store (`state.inlineEditingId`) y los consumidores se SUSCRIBEN (useStoreSlice/selectInlineEditingId — VersoBlock, EditorHotkeys, PropertiesPanel); no hay evento window/iframe que sincronizar porque no hay dos mundos que puentear (el portal comparte árbol React). Cobertura: verso-store.test.ts (slice) + hotkeyMap.test.ts (guard de escritura con inlineEditingId).
- [x] **W37** OverlayBlocker: inyecta CSS mientras hay edición inline activa para que el overlay de selección de Puck no capture clics sobre el editor Tiptap in-situ. — **Verso**: SUPERSEDED POR ARQUITECTURA: el problema que OverlayBlocker parchea (el overlay de dnd-kit DENTRO del iframe comiéndose los clics del editable) no existe — el overlay Verso vive en el documento PADRE como hermano del iframe (FrameController `overlay`), los clics del canvas llegan al documento del iframe directamente (la selección se escucha ahí, VersoEditor onClick capture) y la edición inline reemplaza el render del bloque IN SITU (VersoBlock → VersoInline, sin capa encima). Sin CSS que inyectar porque no hay capa que bloquear. Verificado en vivo en F2 (c418180/3c77930).
- [x] **W38** StablePuckRoot (config.root.render, MODULE-SCOPE a propósito — identidad de componente estable para que React no remonte el iframe entero al recomputar el config): expone window.puckDispatch y window.puckGetData; envuelve en PublicLayoutShell + CanvasThemeTemplate; EditorContext.Provider puentea updateComponent/activeEditorId/setActiveEditorId hacia window.parent.*. — **Verso**: SUPERSEDED — cada responsabilidad tiene su equivalente sin window.*: identidad estable = el handle se crea UNA vez en useState initializer y el registry es memo estable (VersoEditor L265-283); puckGetData/puckDispatch = `handleRef` (EditorHandle vivo: getData/transact/commitInline, W49 testeado en saveWiring.test.ts); PublicLayoutShell + plantilla del tema = la MISMA composición dentro del portal (VersoEditor L1044-1054, W30); el puente EditorContext→window.parent no aplica (portal = mismo árbol React).
- [x] **W39** updateComponent: recorre RECURSIVAMENTE content + cualquier prop que sea array de componentes (slots, ej. columnas) para mergear props por id; despacha vía window.puckDispatch({type:'setData', data: transform, recordHistory:true}) — Puck v0.20 NO es controlado, mutar el mirror local nunca llega al árbol renderizado (fallback pre-dispatch: setData local + onChange). — **Verso**: SUPERSEDED — el doc NORMALIZADO indexa cada nodo por id (`doc.nodes`), así que "mergear props por id en cualquier profundidad" es `tx.setProps(id, patch)` directo, sin recorrido recursivo ni mirrors (el recorrido era un workaround del árbol anidado del fork). Cobertura: verso-commands.test.ts (setProps en nodos anidados) + verso-roundtrip.test.ts (la forma anidada se reconstruye al serializar).
- [x] **W40** migrate(data, config) de @wordjs/puck aplicado al cargar initialData y al importar JSON (migra DropZones legacy a Slots). — **Verso**: SUPERSEDED por `lib/verso/normalize.ts` — la normalización al cargar Y al importar (importDataIntoHandle → replaceData → normalize) absorbe zones legacy FAIL-SOFT (donde migrate lanzaba, normalize conserva y no pierde datos); gate de round-trip sin pérdida en verso-roundtrip.test.ts y el import testeado en paletteActions.test.ts.
- [x] **W41** Root fields distintos por tipo: pageConfig=title/slug/_wjs_template; postConfig=title/slug/category(custom)/allowComments(radio)/_wjs_template/seo_title/seo_description/og_image/noindex. — **Verso**: `rootFieldsPage`/`rootFieldsPost` (lib/verso/coreBlocks.tsx, gate anti-drift en verso-coreBlocks.test.ts) consumidos por pages/[id] y posts/[id] respectivamente vía la prop `rootFields` del VersoEditor; el panel derecho los edita sin selección (vista "Página"/Ajustes) con setRootProps.
- [x] **W42** PuckEditorSkeleton: réplica estructural del shell (header/sidebar/canvas) con animate-pulse mientras isLoading. — **Verso**: reutilizado tal cual por ambas páginas para AMBOS motores (isLoading y también mientras el flag de motor resuelve, 1 frame).
- [x] **W43** EditorLoadError: pantalla BLOQUEANTE (Retry/Volver) si falla la carga del post/page — el editor nunca se monta con contenido vacío guardable. — **Verso**: compartido — el gate loadError corre ANTES del ternario de motor en pages/[id] y posts/[id].
- [x] **W44** editorGuards.unhydratedSaveBlocked({isNew, loaded}): bloquea guardado (manual Y autosave) de un registro EXISTENTE cuyo contenido no hidratado con éxito; siempre permite en registro nuevo. — **Verso**: mismo handleSubmit compartido por ambos motores (el guard corre antes de leer el handle); test de wiring en `editor/__tests__/saveWiring.test.ts`.
- [x] **W45** Gracia post-mount de 800ms (mountedAtRef, DUPLICADA idéntica en pages/[id] y posts/[id]): ignora eventos onChange de inicialización (migrate/resolveData) sin comerse la primera edición real si no dispara evento de init. — **Verso**: el onChange de la rama verso usa el MISMO isWithinPostMountGrace/mountedAtRef en ambas páginas.
- [x] **W46** Rama legacy-HTML: página/post sin _puck_data envuelve su content en bloque HTMLEmbed id determinista HTMLEmbed-legacy-<id>; legacyHtmlRef conserva el HTML original como red de seguridad hasta que el bloque hace round-trip (content.length>0); si el canvas queda vacío Y legacyHtmlRef tiene valor, el guardado preserva el HTML original en vez de vaciarlo. — **Verso**: seedLegacyPuckData en la carga y applyLegacyHtmlFallback en el guardado son COMPARTIDOS (corren antes del ternario de motor); la rama verso del onChange limpia legacyHtmlRef con content>0 y no machaca el cuerpo con lienzo vacío; el fallback con handle vivo está testeado en `saveWiring.test.ts`.
- [x] **W47** Regeneración de fallback HTML (contentRef) en cada onChange, serializando bloques a HTML plano para el campo legacy `content`: pages soporta Heading/Text/Image/Button/Card/Divider/HTMLEmbed; posts SOLO Heading/Text/Image/HTMLEmbed — lógica DUPLICADA y DIVERGENTE entre ambos editores (ya hoy, no solo riesgo futuro). — **Verso**: módulo ÚNICO `lib/verso/contentFallback.ts` con el switch COMPLETO de pages, usado por ambas páginas en la rama verso; byte-igual verificado en `contentFallback.test.ts` (whitespace de Card incluido). DECISIÓN RATIFICADA (F3 ola 2): la divergencia de posts era drift accidental → resuelta hacia el lado completo. El editor legacy conserva sus switches inline intactos (mandato byte-idéntico).
- [x] **W48** Auto-generación de slug desde title (mientras no se edite manualmente), sincroniza root.props.slug tanto en el ref (pages) como en state+ref (posts). — **Verso**: el efecto de autogen es compartido (estado `slug`); en la rama verso el payload lo recoge por la cadena de fallback de handleSubmit (root.props.slug vacío → estado), sin escribir en el handle (no contamina la historia de undo).
- [x] **W49** Guardado: flush window.puckCommitActive() (editor inline abierto) → lee window.puckGetData() (store VIVO, no el mirror local — el deep-equal guard de Puck onChange puede dejar el mirror obsoleto) → payload con meta._puck_data=liveData y meta._wjs_template SIEMPRE enviado incluso '' (el backend mergea meta por clave; omitir deja stale) → POST create o PUT update, con autosave:true en el body si es autosave (servidor salta snapshot de revisión) → window.history.replaceState tras crear (sin remount) para fijar la URL a /admin/pages|posts/<id>. — **Verso**: mismo handleSubmit; la rama verso hace flush con `versoHandleRef.current.commitInline()` y lee `getData()` del HANDLE VIVO (sin mirrors ni window.*); _wjs_template siempre presente verificado con handle real en `saveWiring.test.ts`.
- [x] **W50** Contrato onSave: retorna Promise<boolean> — false=bloqueado/fallido (sin stamp de guardado, sin toast, ni alert en autosave); true/void=éxito (compat). — **Verso**: `editor/saveFlow.ts` (didSaveSucceed) + tests `saveWiring.test.ts` (false, void, excepción en background).
- [x] **W51** Guardia de acceso SOLO en pages/[id] (asimetría, NO existe en posts/[id]): can('edit_posts') redirige a /admin/account si no autorizado. — **Verso**: intacta y ANTES del ternario de motor — aplica igual a ambos; posts sigue sin guardia (asimetría preservada a propósito).
- [x] **W52** Editor de Chrome (/admin/chrome): implementación TOTALMENTE PARALELA — monta <Puck> directo con chromeEditorConfig.tsx, layout 3 paneles fijo (Puck.Components+Puck.Outline / Puck.Preview / Puck.Fields), SIN autosave/revisiones/comentarios/a11y/InlineTiptap/CommandPalette/BlockInserter con patrones/guías/viewport-switcher/withSharedBlockFields; resuelve precedencia site option → theme chrome file → starter template; guarda PUT /api/v1/chrome/:part; valida local (parseChromeData) + servidor (400 con errors[] estructurados); confirmación de 2 pasos para restaurar y para cambiar de parte con cambios sin guardar (window.confirm NUNCA usado — congela el in-app browser). — **Verso (checkpoint W52, oráculo PRIMERO)**: contrato de paridad completo en `documentation/verso/chrome-oracle.md` (bloques/forma del dato/endpoints/validaciones/15 comportamientos C01-C15); mismo flag que pages/posts (`?engine=verso` opt-in, legacy DEFAULT ABSOLUTO, EngineToggle presente) con la rama legacy `<Puck>` byte-intacta en el ternario; `ChromeVersoEditor` (variante delgada: createEditor + FrameController + EditorRenderer + OverlayLayer + DnDDriver + OutlineTree + VersoFieldControl, 3 paneles C08, root wrapper C09 POR REFERENCIA del config legacy) sobre `chromeVersoAdapter` (BlockDefinitions derivados DEL PROPIO buildChromeEditorConfig — fields/render reutilizados por referencia, solo ChromeRow re-adaptado al slot-función con las mismas clases literales; announcement sin ChromeNav gratis; SIN withSharedVersoFields a propósito: allowlist cerrada); guardado/carga/restaurar/dirty COMPARTIDOS (chromeContract.ts: withBlockIds/toContractData movidas byte-idénticas + saveChromeComposition seam del PUT; unhydratedSaveBlocked explícito; el dato de chrome NO es _puck_data — cero guards de meta). Tests `chromeVersoAdapter.test.ts` (13: anti-drift por referencia, defaults por part, slot resolver, round-trip byte-igual de los 3 starters por el store real, transacciones+undo→contrato válido, endpoint espiado con args exactos). Pendiente SOLO gate visual de navegador (DnD/overlay/canvas con tema — igual que el resto de superficies).
- [x] **W53** withSharedBlockFields: envuelve TODOS los bloques (core Y de plugins runtime) con 3 campos comunes — hide (visibilidad por dispositivo, clases wjs-hide-* !important, funciona igual en SSR público y preview editor), anim (animación de entrada + 'scroll' opcional, clamp defensivo duration 100-3000ms/delay 0-3000ms), look (Appearance: fondo/borde/sombra/spacing/tipografía/hover) — wrapper único display:contents (o caja real si hay animación/estilo) para evitar reflow cuando no se usa; defaultProps nuevas: hide:{}, anim:{type:'fade-up',duration:600,delay:0}, look:{}. — **Verso (F3 ola 1, commit 4275aac)**: `lib/verso/sharedFields.tsx` (withSharedVersoFields aplicado en registerCoreBlocks; defaults byte-iguales verificados contra puckConfig en verso-coreBlocks.test.ts; clamp también en la frontera de escritura); el wrapper visual lo pone VersoBlock vía SharedBlockShell (misma implementación que el público). Plugins runtime = F4.
- [ ] **W54** useRuntimePuckConfig: fusiona bloques Puck de plugins de marketplace activos EN TIEMPO DE EJECUCIÓN; primera renderización SSR/hydration devuelve config base sin cambios (evita mismatch); memoizado por identidad estable (config nueva ⇒ Puck reconstruye el store completo Y React remonta el canvas). — **F4 POR MANDATO** (API de bloques para plugins + migrar los 31): el seam existe (BlockRegistry.register acepta bloques en runtime; VersoBlock pinta placeholder fail-soft para tipos sin componente), pero el merge de bloques de marketplace en el registry vivo es el encargo de F4, no de esta ola.
- [x] **W55** replayAnimations(doc): re-dispara animaciones de entrada sobre el Document del iframe bajo demanda (botón header + acción de paleta). — **Verso**: reutilizado TAL CUAL (puro sobre Document, engine-agnóstico) sobre el frameDoc vivo — botón del header (ola 2) + fila de paleta `replay` (ola 3), ambos en `editor/VersoEditor.tsx`; delegación verificada en `paletteActions.test.ts`.
- [x] **W56** canvasGuides.ts: setOutlineMode (contorno 1px discontinuo, sólido en hover) + showSpacingOverlay (bandas padding/margin con etiqueta px, desciende por wrappers display:contents/sole-child hasta el elemento con spacing real) — puro sobre Document, sin dependencia de React ni de Puck salvo el atributo data-puck-component. — **Verso (ola 4)**: NO duplicado — `setOutlineMode` gana un tercer parámetro `blockAttr` (default `data-puck-component`: llamadores legacy byte-idénticos) y Verso pasa `data-wjs-block-id`; `showSpacingOverlay` ya era agnóstico del atributo (recibe el Element) y se reutiliza tal cual. Integración: W06 (VersoGuidesController).
- [x] **W57** Suite de tests del wrapper: SOLO puckHistoryUndoRedo.test.ts ejercita comportamiento real (createAppStore() del fork, debounce 250ms de history.record, back/forward, truncado de rama redo tras nuevo cambio); puckEditorWrapper.test.ts y puckForkDivergence.test.ts son PINES DE TEXTO FUENTE (regex sobre código stripeado de comentarios) — no ejecutan el componente, se invalidan enteros con cualquier reescritura estructural. — **Verso**: SUPERADO CON CRECES (el item describía una deuda del legacy): la suite Verso es CONDUCTUAL de punta a punta — motor (verso-store/verso-commands/verso-roundtrip/verso-dnd-resolve/verso-registry/verso-coreBlocks/contentFallback) + wrapper (saveWiring/saveChipModel/hotkeyMap/blockClipboard/paletteActions/patterns/outlineModel/panelTabs/presence/a11y/actionBar/versoThemeTemplate/viewport) — todos ejecutan código real sobre createEditor/handles vivos, cero pines de texto. 1096 tests frontend verdes en esta ola.

## B. Comportamientos del motor visibles para el autor (fork) — 38 items

- [x] **M01** Reducer con 11 tipos de acción (+ recordHistory opcional en cada una): insert, duplicate, replace, replaceRoot, reorder, move, remove, setUi, setData, set, registerZone/unregisterZone — **Verso**: comandos transaccionales equivalentes (insertNode/removeNode/moveNode/setProps/setRootProps/replaceData sobre `transact`; duplicar = subtreeToItem+regen+insert en actionBarCommands; registerZone no aplica — los slots viven en el doc normalizado). recordHistory ⇒ una transacción = una entrada. Tests: verso-commands.test.ts + verso-store.test.ts.
- [x] **M02** storeInterceptor: auto-decide si graba historial (excluye por defecto registerZone/unregisterZone/setData/setUi/set salvo recordHistory explícito) y SIEMPRE invoca onAction(action, newStatePublic, prevStatePublic) — hook público para observar cada dispatch — **Verso**: `handle.subscribe(listener, selector)` — observación por slice de cada transacción confirmada/undo/redo (el onChange del padre en VersoEditor usa el slice `doc`); selección/drag no graban historia por construcción (no son transacciones). Tests: verso-store.test.ts, saveWiring.test.ts.
- [x] **M03** Store zustand con subscribeWithSelector: state, dispatch, config, componentState (loadingCount por nodo con debounce de 50ms vía setTimeout + pendingLoadTimeouts), plugins, overrides, viewports, zoomConfig{autoZoom,rootHeight,zoom}, status LOADING→MOUNTED→READY (ciclo de vida del boot del iframe), iframe config, selectedItem derivado de ui.itemSelector — **Verso**: store propio con suscripción por selector (useStoreSlice); selección = `selection.nodeId` directo (sin itemSelector compuesto); ciclo de boot del iframe = whenCanvasRootReady (un await, sin máquina de estados); zoom/viewports = viewport.ts + useAreaSize (viewport.test.ts). loadingCount por nodo no tiene consumidor Verso (ver M04). Tests: verso-store.test.ts.
- [ ] **M04** resolveComponentData en el store: resuelve un nodo con callbacks de loading (setComponentLoading 50ms defer) y encadena refreshPermissions tras resolver — **Verso (hueco acotado)**: NO hay seam genérico de resolución async por nodo. Lo que el autor VE está cubierto por bloque: PostsGrid/CategoryPosts hacen fetch cliente en edición (useEditorPosts, coreBlocks L196-217) y Symbol fetch propio (VersoSymbolBlock). El seam genérico (estados de carga, re-resolución) se decide en F4 con los bloques de plugins.
- [ ] **M05** resolveAndCommitData: recorre TODO el árbol una vez, resuelve cada nodo, y solo hace commit (replace/replaceRoot) si el nodo SIGUE existiendo tras la resolución async (guard contra borrado concurrente) y si didChange es true — **Verso (mismo hueco que M04)**: sin pase de resolución global; los datos resueltos JAMÁS se commitean al doc en Verso (viven en el render — de hecho W35 tuvo que replicar el strip del legacy solo por interop). Decisión F4 junto con M04.
- [x] **M06** Slice history: undo/redo con debounce de grabación a 250ms, hasPast/hasFuture, back/forward despachan {type:'set'} con tidyState (limpia ui.field.focus), hotkeys cmd/ctrl+z, shift+z, y (react-hotkeys-hook) — **Verso**: undo/redo/canUndo/canRedo del handle con truncado de rama redo tras nueva edición (verso-store.test.ts); el debounce 250ms es innecesario por construcción (una transacción = una entrada; la escritura inline compone en commitInline); hotkeys Z/Shift+Z/Y en hotkeyMap.test.ts.
- [ ] **M07** Slice permissions: permisos globales por defecto {drag,edit,delete,duplicate,insert}=true, resolvePermissions por item/type/root/todos, cache por id con invalidación por diff (deep-diff getChanged), fuerza refresh con force=true — **Verso (no replicado — SIN CONSUMIDOR, decisión pendiente)**: ni el wrapper legacy ni ningún bloque WordJS resolvía permisos (todo quedaba en el default true); replicar el sistema entero sería API muerta. Si F4 quiere bloques con acciones restringidas, se diseña entonces con consumidor real.
- [ ] **M08** Slice nodes: registerNode/unregisterNode expone métodos imperativos {sync,showOverlay,hideOverlay} por id de nodo (usado por LayerTree para resaltar al hover) — **Verso (hueco pequeño)**: el registro por nodo existe (GeometryStore.registerElement vía onBlockElement) y sostiene overlay/DnD; lo NO portado es la microinteracción hover-en-Estructura → resaltar el bloque en el canvas (OutlineTree hoy solo sincroniza selección en ambos sentidos, outlineModel.test.ts). Estimación: pequeña (OutlineTree onMouseEnter → overlay hover del GeometryStore), pendiente de ola visual.
- [ ] **M09** Slice fields: resuelve campos dinámicamente por componente seleccionado (componentConfig.resolveFields) con abort si el item seleccionado cambió durante la resolución async — **Verso (no replicado — SIN CONSUMIDOR)**: ningún bloque core de WordJS usa resolveFields (los fields del registry son estáticos y PropertiesPanel los renderiza síncrono — por eso pudo eliminar los timeouts 50/300ms de W21). Mismo trato que M07: se diseña si F4 trae un consumidor.
- [x] **M10** DraggableComponent: overlay portaleado (createPortal) fuera del árbol normal, useSortable de dnd-kit con collisionDetector dinámico memoizado por eje, disabled de drag/drop gateado por permisos, ResizeObserver + scroll/resize listeners para re-sincronizar posición, detección automática de eje de arrastre por display:inline computado, click selecciona (dispatch setUi itemSelector), stopPropagation si el click no viene de data-puck-overlay-portal — **Verso**: OverlayLayer + GeometryStore + DnDDriver (F2) cubren el mismo espacio autor-visible sin dnd-kit: overlay en el documento padre, geometría re-sincronizada (GeometryStore observa scroll/resize del iframe), click selecciona (listener capture en frameDoc). Verificado EN VIVO en F2 (c418180) + verso-dnd-resolve.test.ts.
- [x] **M11** DIVERGENCIA 1 (WORDJS, 4 sitios en DraggableComponent + 1 en Preview): overlay del editor portaleado al documento PADRE vía frameElement→[data-puck-preview]→[data-puck-overlay-layer] en vez del <body> del iframe; con fallback al comportamiento upstream si la capa no existe; geometría 1:1 (getBoundingClientRect crudo, sin término de scroll ni división de escala) cuando está en la capa padre; re-sync en scroll (capture:true) + resize del documento del iframe, solo mientras el overlay está visible — **Verso**: esta divergencia ES la arquitectura POR DEFECTO de Verso (lo que el fork parcheaba a mano, Verso lo tiene por diseño): overlay hermano del iframe en el mismo contenedor transformado — comparten sistema de coordenadas y el navegador los escala juntos (contrato viewport.ts, viewport.test.ts + verificación en vivo F2).
- [x] **M12** DIVERGENCIA 2 (WORDJS, 1 sitio): acción 'Edit' en el ActionBar SOLO para componentType Text/Heading, antes de Duplicate, que llama window.puckSetActiveEditorId(id) — bypassa el editor de texto inline nativo de Puck (InlineTextField) en favor del editor de texto enriquecido propio de WordJS — **Verso**: superseded — el ActionBar es PROPIO (overlay/ActionBar.tsx) con acceso completo a id/tipo, y la edición inline se dispara con editSelectedInline (también dblclick en el canvas) sin window.*; test actionBar.test.ts.
- [x] **M13** ActionBar: Action/Group/Label componibles, override completo vía overrides.actionBar (pero sin acceso a id/componentType — limitación de la API pública que motivó la Divergencia 2) — **Verso**: ActionBar propio (W05) con label/subir/bajar/duplicar/eliminar/editar y clamp por modelo; la limitación upstream (sin id/tipo) no existe. Test actionBar.test.ts.
- [x] **M14** DropZone dual-mode: DropZoneEdit (canvas del editor, registra zona on-mount, warning de deprecación si se usa DropZone fuera de root, acceptsTarget por allow/disallow, isEnabled solo si es la zona MÁS PROFUNDA bajo el puntero) vs DropZoneRender (modo publicado/preview, lee de renderContext, expande zones legacy vía setupZone) — **Verso**: VersoSlot (editor) vs ContentRenderer/slotOf (publicado) — la dualidad existe con módulos separados en lugar de un componente bimodal; "la zona más profunda gana" está en el resolver del DnDDriver (verso-dnd-resolve.test.ts); zones legacy las absorbe normalize.ts (verso-roundtrip.test.ts). allow/disallow por zona no tiene consumidor core (los bloques WordJS no lo declaraban) — mismo trato que M07 si F4 lo trae.
- [x] **M15** DropZoneChild: resuelve item real o preview de arrastre (shim desde ZoneStoreContext.previewIndex), aplica pipeline de field-transforms (slot-transform + inline-text-transform + transforms de plugins + transforms de usuario) antes de renderizar — **Verso**: VersoBlock resuelve nodo real o placeholder de drag (state.dragPreview) y aplica las transformaciones equivalentes en el punto de render (slots como funciones, VersoInline en lugar del inline-text-transform); pipeline de transforms de usuario/plugins sin consumidor core (F4).
- [x] **M16** Modelo de datos slot: campos type:'slot' contienen Content (array de ComponentData) directamente dentro de props — reemplaza el modelo legacy de zonas string 'parentId:propName' — **Verso**: la MISMA forma persistida (slots como arrays dentro de props) es el contrato de entrada/salida; internamente el doc se normaliza (nodes por id) y se reconstruye byte-igual — verso-roundtrip.test.ts.
- [x] **M17** Modelo de datos zona legacy (Data.zones: Record<zoneCompound, Content>) TODAVÍA soportado con warning de deprecación en consola — **Verso**: normalize.ts absorbe zones legacy al cargar (fail-soft, sin pérdida) — verso-roundtrip.test.ts.
- [x] **M18** migrate.ts: migración 1 (root bare props → root.props), migración 2 (zonas DropZone → slot) que FALLA DURO (throw) si el slot destino no existe en el config del componente; soporta migrateDynamicZonesForComponent para zonas generadas por arrays dinámicos — **Verso**: ver W40 — normalize.ts hace ambas migraciones FAIL-SOFT (decisión ratificada: conservar sin perder gana a lanzar) — verso-roundtrip.test.ts.
- [x] **M19** resolveAllData: resolución async pura y standalone (fuera del store) de TODO el árbol incluyendo slots anidados recursivamente — para SSR/publish — **Verso**: N/A por arquitectura — el camino de publicación NO cambia con Verso: el resolver SSR real de WordJS (resolveDynamicBlocks) + ContentRenderer siguen siendo los del sitio público (perf F3), nunca dependieron de resolveAllData del fork.
- [ ] **M20** AutoField: 9 tipos de campo (text, number ambos→DefaultField; textarea; select; radio; array recursivo con getItemSummary/min/max; object recursivo; external con fetchList/mapProp/mapRow/filterFields/showSearch; custom con render prop; slot que retorna null y se resuelve fuera por DropZone), shadow local de valor mientras el campo tiene foco (AutoFieldPrivate) para no perder el caret con eco del estado global, onFocus/onBlur sincronizan ui.field.focus, NestedFieldContext propaga readOnlyFields/localName a hijos (Array/ObjectField) — **Verso (PARCIAL)**: VersoFieldControl cubre los 10 tipos (cases text/textarea/number/select/radio/array/object/external/custom/slot, L397-415) incluyendo external con picker inyectado (W22) y custom con render prop (el picker de Symbol lo usa, W35); lo NO auditado aún byte-a-byte: shadow de caret con eco del estado, min/max/getItemSummary de arrays, readOnly anidado — exige pasada dedicada + cotejo en navegador (escribir en un campo mientras el canvas re-renderiza).
- [x] **M21** Drawer: fuente draggable de la paleta (useDraggable de dnd-kit), remonta el id de drag tras cada dragend (workaround de un bug de stale-id en dnd-kit), se registra como droppable type:'void' con collisionPriority:0 SOLO para que NestedDroppablePlugin lo respete como target de cancelación — **Verso**: la paleta es fuente de drag vía `data-wjs-palette-type` (contrato del DnDDriver, sin dnd-kit → los dos workarounds del item no aplican); soltar fuera de una zona válida cancela. Verificado en vivo F2 + W28.
- [x] **M22** Render: renderer puro config+data→React sin NINGUNA dependencia de store/dnd — es el mismo camino de publicación/SSR — **Verso**: ContentRenderer/RenderSubtree (server-safe, cero store/dnd) ES el camino de publicación desde perf F3, y ahora también el de las superficies Verso (miniaturas W27, subtree de Symbol W35).
- [x] **M23** SlotRender + server.tsx: variante headless (con hooks) y variante RSC-safe (sin hooks) del render de contenido de slot — **Verso**: `slotOf` de ContentRenderer (RSC-safe, un wrapper div por slot con el className del contenedor — DOM idéntico a SlotRender) para publicado/miniaturas; VersoSlot para el canvas.
- [x] **M24** AutoFrame: crea el iframe con srcDoc fijo (#frame-root), portalea children dentro, CopyHostStyles clona TODOS los <style>/<link rel=stylesheet> del documento padre al iframe vía MutationObserver en vivo con dedupe por hash (object-hash), sincroniza atributos de <html>/<body> del padre al iframe (para clases de tema/dark-mode), tres estados de disponibilidad (onReady/onNotReady) que alimentan store.status — **Verso**: FrameController + /admin/canvas-frame — el iframe es un DOCUMENTO REAL que posee su propio CSS (orden garantizado, W08) en lugar de clonar el del padre (adiós CopyHostStyles/MutationObserver/dedupe — la fuente de los bugs de posición arbitraria de links); readiness = whenCanvasRootReady (sin polling, sin fugas). Verificado en vivo F2.
- [x] **M25** Puck/index.tsx PuckProvider: construye el AppStore de zustand UNA SOLA VEZ en un useState inicializador — el prop `data` se lee solo en el mount, cambios posteriores al prop NUNCA se releen (data NO controlado); auto-colapsa sidebars en mobile <638px; selecciona el viewport inicial más cercano al ancho de ventana; blending de initialHistory; dedupe de onChange vía fast-deep-equal; dispara resolveAndCommitData() una vez al montar — **Verso**: mismo contrato de no-controlado — createEditor en useState initializer, initialData leído UNA vez (VersoEditor L278-283, comentado); paneles móviles = sheets md:hidden (auto-colapso por CSS); onChange sin dedupe porque solo notifica transacciones confirmadas (no hay ecos que dedupe-ar); resolveAndCommitData no aplica (M04/M05).
- [x] **M26** PuckLayout: layout responsive con sidebars redimensionables (useSidebarResize), auto-ocultación <638px, hotkeys globales monitorizados incluso dentro del iframe (monitorHotkeys(frameDoc)), overrides.puck permite reemplazar TODO el layout, hook de debug dev-only window.__PUCK_INTERNAL_DO_NOT_USE.appStore — **Verso**: el chrome WordJS NUNCA expuso el layout upstream (el wrapper legacy lo reemplazaba entero) — la paridad autor-visible es el layout del blueprint, que VersoEditor implementa (paneles plegables + sheets móviles + hotkeys en window Y frameDoc — EditorHotkeys re-attach en onFrameReady, W03).
- [x] **M27** Sub-exports estáticos Puck.Components/Fields/Outline/Preview — permiten recomponer el layout con las piezas conectadas al store sin heredar el layout por defecto — **Verso**: las piezas SON componentes sueltos conectados al handle (BlockPalette/PropertiesPanel/OutlineTree/EditorRenderer+FrameController) — recomposición libre por construcción; el Verso Lab las monta en otro layout como prueba viviente.
- [x] **M28** DragDropContext: envuelve DragDropProvider de dnd-kit con el preset por defecto + NestedDroppablePlugin propio, AutoScroller opcional (disableAutoScroll), ZoneStore paralelo (zustand separado del AppStore) con zoneDepthIndex/areaDepthIndex/previewIndex (ghost de arrastre) e hoveringComponent — **Verso**: DnDDriver propio sobre pointer events + GeometryStore; el ghost/preview vive en `state.dragPreview` del MISMO store (sin store paralelo que sincronizar). Tests verso-dnd-resolve.test.ts + verificación en vivo F2.
- [ ] **M29** onDragStart/onDragOver/onBeforeDragStart/onDragEnd: distingue drag NUEVO (desde Drawer) vs EXISTENTE (reorder/move), debounce de 100ms en cambio de área con deshabilitación temporal de colisiones fallback (100ms) para evitar parpadeo en cambios de zona, inserción RTL-aware (getDeepDir), rechazo de auto-drop sobre sí mismo o descendientes, commit de datos SOLO tras dragend Y tras que el elemento dnd-kit alcance status idle (post-animación, vía @dnd-kit/state effect()) — **Verso (PARCIAL)**: nuevo-vs-existente (paleta vs canvas), rechazo de auto-drop sobre sí/descendientes (verso-commands.test.ts: moveNode rechaza subtree propio) y commit solo en drop están en el DnDDriver (F2, en vivo); NO auditado: inserción RTL-aware y el anti-parpadeo de cambio de zona (100ms) — cotejo en el gate e2e con documento RTL.
- [x] **M30** NestedDroppablePlugin: resuelve, en cada movimiento de puntero, la zona/área MÁS PROFUNDA bajo el cursor por depth-sort — el mecanismo que hace que zonas anidadas prioricen la más interna — **Verso**: el resolver del DnDDriver prioriza la zona más profunda bajo el puntero — verso-dnd-resolve.test.ts (casos anidados).
- [x] **M31** Colisión dinámica: createDynamicCollisionDetector(dragAxis) — detector consciente de eje (x/y) con cálculo de midpoint-impact geométrico, variante 'directional' más simple de un solo eje — **Verso**: el resolver calcula índice de inserción por midpoint sobre la geometría real (GeometryStore), consciente del eje del layout del slot — verso-dnd-resolve.test.ts.
- [ ] **M32** useSensors: UN solo PointerSensor (nativo de dnd-kit, NO Mouse/Touch/Keyboard clásicos) con activationConstraints diferenciados: mouse-sobre-handle sin restricción extra, touch 200ms delay+10px tolerancia, todo lo demás 200ms delay+10px tolerancia+5px distancia — **Verso (PARCIAL)**: pointer events nativos en el DnDDriver (mouse verificado en vivo F2); los umbrales táctiles diferenciados (200ms/10px/5px) NO están auditados — cotejo en el gate e2e móvil (el modo tap-to-insert de la paleta ya cubre la inserción móvil sin drag, W28).
- [x] **M33** CSS Modules vía postcss-modules + getClassNameFactory (BEM: Root, Root-descendant, Root--modifier) + un puñado de .css globales no-modulares (DraggableComponent/styles.css, Sortable/styles.css, ResizeHandle/styles.css, overlay-portal/styles.css, styles.css raíz, styles/color.css, styles/typography.css) para tokens de diseño — **Verso**: N/A por arquitectura — el chrome Verso usa tailwind + los tokens --ed-* de puck-theme.css (fuente única compartida con el editor actual); no hereda el pipeline CSS del fork porque no depende del fork.
- [x] **M34** Build tsup con 3 entry points: bundle/index.ts (cliente), bundle/rsc.tsx (subset RSC-safe), bundle/no-external.ts — exports condicionales react-server vs default en package.json — **Verso**: N/A por arquitectura — Verso es código fuente de la app (frontend/src), compilado por Next como todo lo demás; la dualidad cliente/RSC se resuelve por módulos ("use client" en el chrome, ContentRenderer server-safe).
- [x] **M35** API pública headless: usePuck/createUsePuck/useGetPuck sobre un store zustand ESPEJO (generateUsePuck) del AppStore real, expone appState (público, sin indexes), config, dispatch, getPermissions, refreshPermissions, selectedItem, getItemBySelector/ById, getSelectorForId, history{back,forward,setHistories,setHistoryIndex,histories,index,hasPast,hasFuture} — **Verso**: EditorHandle ES la API headless — getData/getDoc/getState/transact/subscribe(selector)/select/undo/redo/canUndo/canRedo/replaceData/commitInline, SIN store espejo (el espejo era la fuente del bug de mirrors obsoletos que W49 tuvo que esquivar). useStoreSlice como hook. Tests: verso-store.test.ts + todo el wrapper lo consume.
- [x] **M36** lib/data/*: walkTree, walkAppState (motor de indexación state.indexes.{nodes,zones} recalculado en cada acción), mapFields (visitor genérico por field.type), flatten-data/flatten-node (vista plana con defaultProps mergeados para diffing), get-item, insert/remove/reorder/replace (primitivas de splice inmutable), set-deep (setter por dot-path), setup-zone (zones legacy→content array), strip-slots/to-component/to-root (conversores de forma), populate-ids (asigna ids faltantes), for-related-zones/get-ids-for-parent/find-zones-for-area (bookkeeping de zoneCompound), make-state-public (quita indexes antes de exponer AppState) — **Verso**: lib/verso/commands.ts + normalize.ts son el equivalente (subtreeToItem/collectSubtreeKeys/emitNodeProps, primitivas insert/remove/move/setProps, normalización de zones e ids faltantes); el ÍNDICE por id no se "recalcula en cada acción": el doc normalizado ES el índice. Tests verso-commands/verso-roundtrip.
- [x] **M37** Overrides públicos (10 puntos de extensión): header, actionBar, headerActions, preview, fields, fieldLabel, drawer, drawerItem, iframe, outline, componentOverlay, puck, fieldTypes — actionBar NO recibe id/componentType (limitación real que forzó la Divergencia 2) — **Verso**: sin API de overrides A PROPÓSITO — el ÚNICO consumidor de overrides era el propio wrapper WordJS, que ahora ES Verso (header/actionBar/fields/outline/iframe son componentes propios). La frontera de extensión para plugins sigue siendo declarar BLOQUES, no overrides del chrome (misma política que el legacy); si F4 exigiera más, se diseña con consumidor real.
- [x] **M38** Contrato de generación de id: cada ComponentData recibe props.id único vía generateId() (basado en uuid) al insertar o migrar (populate-ids), usado como clave en indexes.nodes y como valor de data-puck-component/data-puck-dnd en el DOM — **Verso**: crypto.randomUUID en cada frontera de inserción (insertType/insertMediaItem/patrones/pegar — regenIds recursivo del legacy al pegar/patrones), el id ES la clave de doc.nodes y el valor de data-wjs-block-id en el DOM (VersoBlock). Tests: blockClipboard.test.ts (regen sin colisiones), patterns.test.ts, verso-roundtrip.test.ts (ids faltantes se materializan).

## Estado

**F3 ola 2 (2026-08-15) — wrapper VersoEditor núcleo.** Marcado SOLO lo evidenciado con test de node
o código verificable (la verificación visual pantalla-a-pantalla queda para el gate de navegador del
orquestador). Nuevo en esta ola:

- `frontend/src/components/verso/editor/` — VersoEditor.tsx (chrome completo del blueprint),
  SaveStateChip, EditorHotkeys, BlockPalette, OutlineTree, PropertiesPanel + módulos puros
  panelTabs/saveChipModel/hotkeyMap/outlineModel/saveFlow (todos con test).
- `frontend/src/lib/editorEngine.ts` — flag de motor (query ?engine= > localStorage
  wjs_editor_engine > NEXT_PUBLIC_WORDJS_EDITOR_ENGINE > legacy DEFAULT ABSOLUTO), matriz testeada.
- `frontend/src/lib/verso/contentFallback.ts` — fallback HTML compartido (W47 resuelto al lado
  completo, byte-igual testeado).
- pages/[id] y posts/[id]: ternario de motor — rama legacy INTACTA (mismo árbol, mismas props);
  rama verso con las mismas props de datos + guards compartidos + versoHandleRef (getData vivo).
- Mecanismos legacy que Verso SUPERSEDE por arquitectura (no se portan 1:1, el contrato queda
  cubierto por el store): W36 (CustomEvent puck-editor-change → `state.inlineEditingId` +
  suscripción), W38/W39 (window.puckDispatch/puckGetData/updateComponent → EditorHandle vía
  `handleRef` + transact/setProps recursivo por diseño del doc normalizado), W40 (migrate → 
  normalize.ts con round-trip fail-soft, gate verso-roundtrip.test.ts).

**F3 ola 3 (2026-08-15) — CommandPalette + clipboard + patrones.** Nuevo en esta ola (todo con
test de node; gates vitest 1072/0, tsc 0, eslint 0 sobre los ficheros tocados):

- `editor/VersoCommandPalette.tsx` + `editor/paletteActions.ts` — la paleta ⌘K COMPLETA (W29,
  reemplaza el placeholder), combobox ARIA correcto, acciones con la espec del legacy + filas
  nuevas subir/bajar/Ver estructura; export/import JSON (import = replaceData, UNA entrada de
  undo, confirm modal — nunca window.confirm).
- `editor/blockClipboard.ts` — Ctrl+C/V de bloques (cierra W03) + copiar/pegar estilos (W26):
  claves y forma EXACTAS del legacy (interop cross-pestaña y cross-EDITOR), ids regenerados
  recursivamente al pegar con el regenIds compartido.
- `editor/patterns.ts` + `editor/PatternsPanel.tsx` — pestaña Plantillas real del rail (W27
  parcial: falta solo la miniatura <Render> en vivo) + los 3 quick-picks del estado vacío (W19).
- puckI18n.ts: 3 tripletas nuevas (Subir bloque/Bajar bloque/Ver estructura); retiradas las 2 de
  los placeholders de paleta/plantillas que ya no existen.
- hotkeyMap: Ctrl+C→copy / Ctrl+V→paste (test actualizado — antes pineaba que eran null).

**F3 ola 4 (2026-08-15) — superficies restantes.** Todo con test de node donde hay lógica pura;
gates: vitest 1091/0, tsc 0, eslint 0 errores sobre los ficheros tocados. Nuevo en esta ola:

- `canvas/VersoThemeTemplate.tsx` (W30) — la plantilla del tema envuelve el canvas: misma cadena
  fail-closed que el resolver público, dropdown _wjs_template re-envuelve EN VIVO leyendo el root
  del store; test `versoThemeTemplate.test.ts`. pages/[id]/posts/[id] pasan templateKind (solo
  rama verso).
- `editor/presence.ts` (W09) — heartbeat COMO MÓDULO + chip ámbar del header; test
  `presence.test.ts` (timers falsos).
- Recursos/Notas/Historial del rail REALES (W22/W24/W23) reutilizando MediaPickerModal /
  ReviewComments / RevisionsSidebar (los tres auditados agnósticos — reuso directo, sin wrapper);
  `renderExternalPicker` cableado a MediaPickerModal + rememberPickedMedia vía PropertiesPanel.
- `editor/a11y.ts` (W20/W25) — runA11yAudit COMPARTIDO parametrizado por atributos (default
  legacy intacto) re-apuntado a data-wjs-block-id; drawer 340px con A11yPanel compartido; test
  `a11y.test.ts` (DOM stub; contraste → gate de navegador).
- Guías REALES (W06/W56) — canvasGuides compartido con `blockAttr` opcional; VersoGuidesController
  + botón del header activo.
- Paleta ⌘K: filas media/revisions/comments/a11y/guides con ids/glifos del legacy
  (paletteActions.ts, test actualizado); retiradas las 4 tripletas de placeholders ya sin uso.

Pendiente para olas 5+ (documentado, nunca omitido en silencio): símbolos-acción de paleta (W35 —
su fila de paleta llega con la superficie), miniatura en vivo de patrones (resto de W27), i18n
localizeConfig sobre el registry completo (W32 — el chrome Verso ya pasa todo por trStr),
InlineTiptap completo (W34 — la base VersoInline es de F2), W08 PreviewFrame (inyección CSS/escala
ya cubierta por FrameController/canvas-frame a falta de cotejo visual). La verificación
pantalla-a-pantalla de todas las superficies de esta ola (plantilla envolviendo el canvas, guías
pintadas, contraste a11y, drawers) queda para el gate de navegador del orquestador.

**Checkpoint W52 (2026-08-15) — unificación del editor de chrome (/admin/chrome) sobre Verso.**
Gates: vitest 1109/0 (66 files, +13 nuevos), tsc 0, eslint 0 sobre los ficheros tocados. ORÁCULO
PRIMERO: `documentation/verso/chrome-oracle.md` (contrato de paridad: 9 bloques con sus props/
defaults, forma { root, content } en option site_chrome_<part> vía PUT/DELETE /api/v1/chrome/:part,
validación local parseChromeData + backend chrome-validate con errors[] estructurados, y los
comportamientos C01-C15 del editor actual). Implementación:

- `app/admin/chrome/chromeVersoAdapter.tsx` — BlockDefinitions derivados DEL PROPIO
  buildChromeEditorConfig (fields/render POR REFERENCIA — un render de Puck es un function
  component; los wrappers de binding Logo/Nav/… funcionan tal cual, sin exportarlos ni
  duplicarlos); ChromeRow re-adaptado al slot-función de Verso con las mismas clases literales;
  root wrapper C09 reutilizado por referencia; SIN withSharedVersoFields (allowlist cerrada del
  contrato). `createChromeVersoSetup(part)` = registry + componentMap + isSlot + RootWrapper.
- `app/admin/chrome/ChromeVersoEditor.tsx` — variante DELGADA (a propósito no VersoEditor: el
  dato de chrome no es _puck_data — sin autosave/presencia/revisiones/plantillas de tema):
  createEditor + FrameController (/admin/canvas-frame ya carga wordjs-ui.css + tema activo → C07
  sin poll de 700ms) + EditorRenderer + OverlayLayer + DnDDriver + OutlineTree + VersoFieldControl,
  3 paneles con las clases del legacy (C08). Mejoras documentadas (oráculo §6): tap-to-insert y
  Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y.
- `app/admin/chrome/chromeContract.ts` — withBlockIds/toContractData MOVIDAS byte-idénticas de
  page.tsx (un solo productor para ambos motores) + saveChromeComposition (seam 1:1 del PUT).
- `app/admin/chrome/page.tsx` — mismo patrón de flag que pages/posts (resolve tras montar,
  re-resuelve en navegación suave, EngineToggle presente, legacy DEFAULT ABSOLUTO); la rama legacy
  `<Puck>` byte-intacta en el ternario; carga/dirty/guardar/restaurar/banners COMPARTIDOS;
  unhydratedSaveBlocked explícito en handleSave; la inyección de hojas de 700ms gated a legacy
  (el canvas Verso resuelve sus propias hojas — evita pelea de hrefs).
- Tests `app/admin/chrome/__tests__/chromeVersoAdapter.test.ts` (13): anti-drift por referencia
  contra chromeEditorConfig (tipos por part, labels, fields, defaults — incluidos los defaults de
  ChromeNav dependientes del part —, announcement sin ChromeNav), clases literales del ChromeRow
  Verso, slot resolver, round-trip byte-igual de los 3 starters por el createEditor REAL,
  transacciones anidadas + undo → contrato siempre válido, y guardado con endpoint espiado (args
  exactos + default chromeApi.save + propagación de errors[]). La matriz del flag ya estaba
  testeada (editorEngine.test.ts) y aplica sin cambios.
- VERIFICADO EN NAVEGADOR en esta misma pasada (dev :3000, ?engine=verso): legacy default intacto,
  toggle, selección+ActionBar, edición en vivo, DnD paleta→Fila, Ctrl+Z, cambio de part con/sin
  guard de descarte. La pasada cazó y corrigió 3 defectos invisibles a node (oráculo §7): click en
  bloque-enlace navegaba el admin (preventDefault en capture; el mismo riesgo en VersoEditor queda
  spawneado como task aparte), panel de campos stale tras undo (suscripción al NODO), y dirty en
  falso positivo permanente por el orden de claves de la serialización Verso (onInit re-basa el
  baseline; test nuevo del invariante — 14 en total). Queda para el gate del orquestador: PUT/DELETE
  reales (no mutar el sitio local), canvas del announcement, viewport estrecho. Gates finales:
  vitest 1110/0, tsc 0, eslint 0.

**F3 ola 5 (2026-08-15) — paridad final del wrapper (cierre de la sección A).** Gates: vitest
1096/0, tsc 0, eslint 0 sobre los ficheros tocados. Nuevo en esta ola:

- **W35 símbolos COMPLETO**: fila `save-symbol` en la paleta ⌘K (id/glifo/label/posición del
  legacy) sobre `saveSelectedAsSymbol` (paletteActions.ts — subtreeToItem + strip de
  SYMBOL_RESOLVED_PROPS + nombre `<label> · <HH:MM>` + guard anti-Symbol), cableada en
  VersoEditor con symbolsApi.create + toast/alert; 4 tests nuevos en paletteActions.test.ts.
  El flujo insertar→picker→subtree (cap 1) ya existía (coreBlocks L953 + VersoSymbolBlock).
- **W27 miniaturas COMPLETO**: `VersoPatternPreview` (PatternsPanel.tsx) — anatomía del legacy
  (110px/1180/scale 0.236/fade) con `RenderSubtree` compartido, aria-hidden +
  pointer-events-none; los patrones de usuario previsualizan `userPatternItems` (patterns.ts,
  test) = exactamente lo que insertará.
- **W08 COMPLETO**: canvas-frame gana la scrollbar delgada byte-igual al legacy y la
  re-resolución del tema activo cada 10s con swap imperativo sin FOUC (link tras el actual →
  onload → retirar; orden framework→tema intacto).
- **W32/W33 evidenciados**: labels de bloque por trStr en BlockPalette y VersoCommandPalette
  (reactivos al idioma sin remontar el registry; la búsqueda matchea lo mostrado).
- **W36-W40, W57 marcados** con su evidencia de supersesión (store/normalize/handleRef — ya
  ratificada en la ola 2, ahora contabilizada item a item).
- **Sección B anotada item a item**: 29/38 marcados con evidencia (test de node, verificación en
  vivo de F2 o supersesión por arquitectura); los 9 abiertos (M04/M05 resolvers async, M07
  permisos, M08 hover-outline, M09 resolveFields, M20 sutilezas de AutoField, M29 RTL/anti-
  parpadeo, M32 umbrales táctiles) quedan con diagnóstico y dueño (F4 / gate e2e / decisión).

HUECOS ABIERTOS de la sección A (justificados, jamás recorte silencioso): **W34** (BubbleMenu
inline completo — bloqueado por la decisión F3.5 de reemplazar Tiptap: portarlo ahora sería
trabajo desechable) y **W54** (bloques de plugins en runtime — F4 por mandato). W52 lo cerró en
paralelo el checkpoint W52 de esta misma fecha (ChromeVersoEditor — ver su bloque arriba).
La verificación pantalla-a-pantalla de las superficies de las olas 4-5 (plantilla, guías,
contraste a11y, drawers, miniaturas, flujo de símbolos con backend real) queda para el gate de
navegador del orquestador.

La seccion B se valida en el lab/e2e (muchos items ya cubiertos por F2 — marcar con referencia al
commit/test).
