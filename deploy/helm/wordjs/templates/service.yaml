apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordjs.fullname" . }}
  labels:
    {{- include "wordjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordjs.selectorLabels" . | nindent 4 }}
