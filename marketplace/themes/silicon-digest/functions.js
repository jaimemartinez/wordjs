/**
 * Silicon Digest Theme Hooks & Dynamic Functions (GLASSMORPHISM Archetype)
 */

module.exports = function registerThemeHooks(wp) {
    console.log('[Theme: Silicon Digest] (glassmorphism) Registered successfully.');
};
