// react-import.js
// Injected by tsup into every module (see tsup.config.ts `inject`) so JSX resolves `React`
// without each source file importing it. Vendored verbatim from Puck v0.20.2's build config.
import React from "react";

export { React };
